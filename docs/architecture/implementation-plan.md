# Implementation plan — first vertical slice

This is a living tracking document. Update it at the end of every meaningful session: record what was completed, the exact next smallest task, and any unresolved decisions.

See `docs/product/mvp.md` for what the slice must do and `docs/architecture/decisions/` for why it's built this way.

## Architecture recap

Single Next.js (TypeScript) app, deployed on Render (web) with Neon (Postgres) once a public deployment is needed (ADR-0001; see session 17's 2026-08-25 log entry for the hosting choice). The first slice runs analysis **synchronously and in-process** — no separate worker entrypoint or job queue yet; that design is specified in ADR-0004 but its implementation is deferred until there's a concrete reason (see "Sequencing" below). Source acquisition uses GitHub tarballs (ADR-0002), JavaScript/TypeScript analysis uses ts-morph syntactically (ADR-0003), and results are keyed by `(owner, repo, commit_sha, analyzer_version)` (ADR-0005) once persistence is built. Python analysis needs its own ADR (library/approach) before that work starts — ADR-0003 currently covers JavaScript/TypeScript only.

## Sequencing (revised 2026-07-22 for TypeScript/JavaScript/Python scope)

`docs/product/mvp.md` expanded the supported ecosystem from TypeScript+React only to TypeScript, JavaScript, and Python sharing one language-neutral Lore model, removed scheduled/automatic refresh and health scores/findings from MVP scope, and introduced explicit Detected/Inferred/Unknown/Unsupported certainty categories. `docs/product/mvp.md`'s own recommended sequence is: define the shared Lore model, build JavaScript/TypeScript extraction, validate Start Here and major-area detection against representative repositories, add Python extraction against the same contract, validate Python at the same threshold, test mixed-language repositories, then launch only when every advertised language meets the minimum value contract. This plan follows that sequence.

It also preserves the resequencing from the earlier product-scope review (2026-07-22), which found that building acquisition, persistence, and async-job plumbing before proving the analyzer produces a useful lore inverted the "smallest end-to-end slice" principle. ADR-0001 and ADR-0004 mark the worker/job-queue design as deferred-until-needed rather than a session-1–4 requirement; ADR-0002 defers PAT provisioning to the session that first calls the GitHub API. Managed Postgres provisioning and hosting account setup remain deferred to the sessions that first need them.

## Acceptance criteria for the slice

1. Submitting a valid public GitHub URL analyzes it and shows the result at `/lore/{owner}/{repo}` (synchronously for the first slice; may show a brief loading state).
2. An invalid URL or nonexistent repo produces a clear, honest error — no silent failure.
3. The app resolves the default branch and HEAD commit SHA via an authenticated GitHub API call.
4. The app fetches the repo tarball at that commit and extracts it under enforced size/file-count/time limits, rejecting path-traversal entries.
5. The app detects whether the repo is a supported TypeScript, JavaScript, or Python project, including whether React is present as framework-aware enrichment within a JS/TS project; unsupported repos get an honest "not supported" result, not a crash or fabricated analysis.
6. Source file discovery excludes `node_modules`, build/dist output, virtual environments, and other generated/vendored paths for each supported language.
7. The extracted project model captures project type, source files, per-file imports/exports, internal dependency edges, probable entry points, public surfaces, test relationships, and confidently-detected React components, via syntactic (non-type-checked) parsing — populating one shared, language-neutral Lore model rather than separate per-language report formats.
8. Derived views are computed: repository orientation, a Start Here reading path (~3–7 items, each with rationale, evidence, and a certainty label), a major-area model, entry points, and direct relationships. No health scores or generalized health findings are computed for the MVP.
9. Results are persisted keyed by `(owner, repo, commit_sha, analyzer_version)`; re-analysis at the same commit+analyzer version is idempotent.
10. `/lore/{owner}/{repo}` renders the latest completed run as plain tables/lists, shows the analyzed commit SHA, timestamp, and analyzer version, and links each major claim back to the relevant file on GitHub with a Detected/Inferred/Unknown/Unsupported certainty label.
11. A manual "re-analyze" action re-runs analysis, rate-limited. No scheduled or automatic refresh is implemented for the MVP.
12. An adversarial fixture (oversized files, deep nesting, tar path-traversal attempt) fails safely within limits instead of hanging or crashing the process.
13. JavaScript/TypeScript and Python fixtures each independently satisfy the minimum value contract (orientation, Start Here, major areas, entry points, relationships, evidence/gaps) before the slice is considered launch-ready; a representative mixed-language fixture is presented as one repository-level model without inventing unevidenced cross-language relationships.

Async dispatch (job queue, worker process, crash reaper — ADR-0004) is intentionally not an acceptance criterion for this slice; it's added once synchronous analysis demonstrably needs to become non-blocking.

## Fixture strategy

- `fixtures/ts-react-app/` — hand-built TS+React app: tsconfig with a path alias, one entry point, 2–3 components, one utility module, a `dist/`-like directory to verify exclusion.
- `fixtures/ts-library/` — small plain-TypeScript (non-React) library shape.
- `fixtures/python-app/` — conventional Python application: `pyproject.toml`, `src/` layout, a console-script entry point, a couple of internal modules.
- `fixtures/python-library/` — small Python package with a public surface and a `pytest` test directory.
- `fixtures/unsupported/` — missing/unusual configuration for both language families, exercises the honest "partially supported" path.
- `fixtures/mixed-language/` — small repo containing both a JS/TS project and a Python project, to exercise the repository-level model without inventing cross-language relationships.
- Manual (non-automated) sanity check against 1–2 pinned small real public repos per language, once fixtures pass for that language — pins to be chosen and recorded here later in the plan.

## Sessions (~60–120 min each)

- [x] 1. Scaffold Next.js + TypeScript app, lint/format config, minimal local env config, liveness-only health-check route (no DB, no PAT, no hosting decisions).
- [x] 2. Define the shared, language-neutral Lore model (Repository, AnalysisSnapshot, Project, SourceLocation, StructuralArea, EntryPoint, Relationship, PublicContract, TestRelationship, Recommendation, Evidence, Gap) and the minimum value contract as code-level types, independent of any language extractor.
- [x] 3. Build the two JS/TS local fixture repos; get ts-morph reading a fixture directly from local disk, with source file discovery + exclusion rules.
- [x] 4. ts-morph project model extraction against local fixtures: imports/exports, internal dependency edges, entry-point heuristics, public surface, test relationships, React component detection.
- [x] 5. Derived views against JS/TS fixtures: Start Here (with rationale, evidence, and certainty per item), major areas, entry points, direct relationships — rendered on a plain unstyled page. This is the session that proves or disproves the core "wow moment" for JS/TS.
- [x] 6. Validate Start Here and major-area detection against a few pinned real public JS/TS repos; adjust heuristics before moving to acquisition or Python. The two JS/TS repos to test should be:
  - https://github.com/sindresorhus/globby
  - https://github.com/stemmlerjs/simple-typescript-starter
  - https://github.com/pieces-app/example-typescript
  - https://github.com/Adedoyin-Emmanuel/react-weather-app
- [x] 7. GitHub URL input, validation/normalization, GitHub API client (default branch + HEAD SHA) — first use of a GitHub PAT.
- [x] 8. Tarball fetch + safe extraction (size/file-count/time limits, path sanitization) into temp dir, with cleanup; swap the JS/TS analyzer's input from local fixtures to a fetched repo.
- [x] 9. DB schema + migrations: `repos`, `analysis_runs` (local Postgres or SQLite is enough; managed provider not needed yet).
- [x] 10. Persist `analysis_run`, idempotent on `(owner, repo, commit_sha, analyzer_version)`; render `/lore/{owner}/{repo}` from persisted JS/TS results with evidence links and certainty labels.
- [x] 11. Manual re-analysis action (rate-limited) + honest unsupported/failed states.
- [x] 12. Write an ADR for the Python analysis approach (library and syntactic-vs-typed tradeoff, mirroring ADR-0003), then build the Python extractor against the same shared Lore-model contract: `fixtures/python-app`, `fixtures/python-library`, imports/packages, `pyproject.toml`/`setup.py`/`setup.cfg`, console-script and conventional entry points, `pytest`/`unittest` test relationships.
- [x] 13. Validate Python Start Here and major-area output against the same usefulness threshold as JS/TS, using local fixtures and 1–2 pinned real public Python repos. Examples of basic python repos include:
  - https://github.com/HypothesisWorks/hypothesis
  - https://github.com/spulec/freezegun
- [x] 14. Wire real language-detection dispatch into `analyze-and-persist.ts` (ADR-0007: GitHub `languages` API signal, 10% byte-share threshold, multi-analyzer dispatch, explicit unsupported-language state) and confirm the repository-level model presents JS/TS and Python projects together without inventing unevidenced cross-language relationships.
- [x] 15. Automated tests: analyzer correctness against all fixtures (JS/TS, Python, mixed, unsupported), evidence traceability, failure handling (malformed, oversized, path-traversal attempt).
- [x] 16. _(Only if needed by then)_ Async dispatch: `analysis_jobs` table, worker entrypoint, `FOR UPDATE SKIP LOCKED` claim, stuck-job reaper (ADR-0004's deferred design). **Skipped — see 2026-07-28 log entry.**
- [x] 17. Deploy: Render (web) + Neon (Postgres), wire secrets, first public deploy.
- [ ] 18. Local dev docs, run-through against fixtures + pinned public repos across all three languages, self-review against `docs/product/mvp.md`'s minimum value contract and success test (Phase 3 of the founding brief).
- [x] 19. Real data for the Architecture page (ADR-0008): replace `src/lib/fixtures/payments-service.ts` example content at `/lore/{owner}/{repo}/architecture` with Major Areas, direct Relationships (area-to-area, derived from existing `StructuralArea` id lists), Entry Points, and `Project.frameworks`, rendered as evidence-backed lists/tables — no diagram, infrastructure detection, or boundary-narrative claims.

## Progress log

### 2026-07-22 — Phase 1 complete

Completed: product thesis, risk identification, architecture decision (single deployable + worker, tarball acquisition, ts-morph syntactic analysis, Postgres job queue, result keying), five ADRs, acceptance criteria, fixture strategy, and session breakdown. Architecture validated against solo-maintainer constraints via `product-scope-guardian`. No application code written yet.

### 2026-07-22 — Scope review and resequencing

A second `product-scope-guardian` review of the completed foundation found the original session order (DB + job queue + worker + tarball acquisition, sessions 2–5) front-loaded distributed-systems plumbing before any code proved the analyzer itself is useful — inverting the "smallest end-to-end slice" principle. Revised ADR-0001 and ADR-0004 to mark the worker/job-queue design as deferred-until-needed rather than a session-1–4 requirement; revised ADR-0002 to move PAT provisioning to the session that first calls the GitHub API. Resequenced sessions so the analyzer is proven against local fixtures before acquisition, persistence, and before async dispatch/hosting/deploy. No product requirement changed — only build order and what's provisioned when.

### 2026-07-22 — MVP scope expanded to TypeScript, JavaScript, and Python; plan rewritten

`docs/product/mvp.md`, `docs/MANIFESTO.md`, `docs/PRINCIPLES.md`, `docs/MODEL_OUTPUT.md`, and `PROMPT.md` were rewritten as the new product-foundation source of truth, expanding the supported ecosystem from TypeScript+React only to TypeScript, JavaScript, and Python sharing one language-neutral Lore model; removing scheduled/automatic refresh and health scores/generalized health findings from MVP scope; and introducing explicit Detected/Inferred/Unknown/Unsupported certainty categories. This implementation plan, `CLAUDE.md`, `README.md`, `docs/product/mvp.md`, ADR-0003, and the `.claude/agents/` subagents were rewritten to match. ADR-0003 now explicitly scopes ts-morph analysis to JavaScript/TypeScript only; Python analysis requires its own ADR before session 12 begins. The session breakdown was resequenced to prove JS/TS Start Here/major-area detection first, then add Python against the same contract, per `docs/product/mvp.md`'s recommended implementation sequence. The cron-triggered scheduled-refresh session was removed entirely, since scheduled refresh is now an explicit MVP exclusion rather than a deferred feature.

### 2026-07-22 — Correction: Session 1 was not actually complete

A previous entry in this log claimed Session 1 (Next.js scaffold with `create-next-app`, App Router, TypeScript, ESLint, a liveness-only `/api/health` route) was complete. That claim did not match the working tree: no `src/`, `next.config`, lockfile, or `node_modules` exist, and no `next` dependency is declared anywhere. The only file present was a stray, dependency-free stub `package.json` (apparently left over from an unrelated `npm init`), whose `repository`/`bugs`/`homepage` URLs were also malformed (a literal space where the repo slug belongs). That log entry has been removed as inaccurate; the stub `package.json`'s URLs were corrected to point at the real remote (`nielse63/RepoLore`) but no scaffold, dependencies, or scripts were added. Session 1 is the actual next task.

### 2026-07-22 — Session 1 complete: Next.js scaffold

Scaffolded via `create-next-app` (App Router, TypeScript, ESLint, no Tailwind, `src/` dir, `@/*` import alias) into an isolated temp directory, then merged into the repo root rather than running the generator in place, since the repo already had a real `package.json`, `README.md`, and `.gitignore` that a direct in-place run would have conflicted with or overwritten. The pre-existing `.gitignore` already covered `node_modules/`, `.next`, and `.env*` (with a `!.env.example` exception), so it needed no changes. `package.json`'s prior metadata (`name`, `description`, `repository`, `author`, `license`, `bugs`, `homepage`) was preserved; the stub `main`/`directories`/`type: commonjs` fields and the failing placeholder `test` script were dropped, and `dev`/`build`/`start`/`lint` scripts plus the generated `next`/`react`/`typescript`/`eslint` dependencies were added. Added `.env.example` (empty) and a liveness-only `src/app/api/health/route.ts` (no DB check). Verified locally: `npm run build` succeeds, `npm run lint` passes clean, and `npm run dev` serves `GET /api/health` with `200 {"status":"ok"}`.

`npm install` reports 3 pre-existing transitive vulnerabilities (moderate/high) in `postcss`/`sharp`, pulled in by Next.js itself; `npm audit fix --force`'s only offered fix is downgrading to `next@9.3.3`, which is not a real fix. Left as-is; revisit if `npm audit` offers a real fix at the current Next major version.

**Next smallest task:** Session 2 — define the shared, language-neutral Lore model (Repository, AnalysisSnapshot, Project, SourceLocation, StructuralArea, EntryPoint, Relationship, PublicContract, TestRelationship, Recommendation, Evidence, Gap) and the minimum value contract as code-level types, independent of any language extractor.

**Unresolved decisions (deferred on purpose, not blocking):** exact hosting provider (Fly.io vs. Railway) and managed Postgres provider (Neon vs. Railway Postgres) — deferred to session 17 (deploy); Python analysis library/approach — deferred to session 12's ADR.

### 2026-07-23 — Session 2 complete: shared Lore model

Added `src/lore/model.ts` with the full shared, language-neutral domain model described in `docs/MODEL_OUTPUT.md` ("Shared Lore Domain Model"): `Repository`, `AnalysisSnapshot`, `Project`, `SourceLocation`, `StructuralArea`, `EntryPoint`, `Relationship`, `PublicContract`, `TestRelationship`, `Recommendation`, `Finding`, `Evidence`, `Gap`, plus the aggregate `Lore` type and a shared `CertaintyCategory` (`detected`/`inferred`/`unknown`/`unsupported`). No extractor, persistence, or UI code depends on these types yet — they exist independent of any language extractor, per the session scope.

Also encoded the MVP's minimum value contract (`docs/product/mvp.md`, "Minimum Value Contract") as a `MinimumValueContract` type plus `evaluateMinimumValueContract(lore)`, which checks repository orientation, a Start Here path of 3–7 items each with evidence, a major-area model, entry points, direct relationships, and traceable evidence (every piece of evidence has a source location). This gives session 15 (automated tests) and session 13 (Python validation against the same threshold) a concrete, checkable contract rather than only a documented one. `npx tsc --noEmit` and `npm run lint` both pass clean.

**Next smallest task:** Session 3 — build the two JS/TS local fixture repos (`fixtures/ts-react-app/`, `fixtures/ts-library/`); get ts-morph reading a fixture directly from local disk, with source file discovery and exclusion rules (`node_modules`, build/dist output).

### 2026-07-23 — Session 3 complete: JS/TS fixtures + ts-morph source discovery

Added `ts-morph` (dependency) and `tsx` (devDependency, for running TypeScript scripts without a build step ahead of session 15's test suite).

Built `fixtures/ts-react-app/` (own `tsconfig.json` with a `@/*` path alias, `src/index.tsx` entry point, `App.tsx` composing two components under `src/components/`, `src/utils/format.ts`, and a `dist/bundle.js` stub to verify build-output exclusion) and `fixtures/ts-library/` (plain TypeScript, no React: `src/index.ts` re-exporting a public surface from `src/math.ts`, which itself depends on an internal, non-exported `src/internal/round.ts` — giving session 4 both a public/internal boundary and an internal dependency edge to extract). Each fixture has its own standalone `package.json`/`tsconfig.json` and is excluded from the root app's TypeScript program (`tsconfig.json` `exclude`) since it's a fixture, not application code.

Added `src/analysis/js-ts/discovery.ts`: `discoverSourceFiles(rootDir)` builds a ts-morph `Project` directly from a glob over `rootDir` (`.ts`/`.tsx`/`.js`/`.jsx`), with glob-negation plus a defensive filename-segment check excluding `node_modules`, `dist`, `build`, `out`, `.next`, `coverage`, and `.git` at any depth. Per ADR-0003, this stays syntactic-only — no `tsConfigFilePath`-driven, type-checked `Program` is constructed; only the minimal compiler options needed to parse JSX/TS syntax.

Verified manually (no test suite yet — session 15) via `npm run discover -- <fixture-path>` (`scripts/discover-fixture.ts`): `ts-react-app` correctly discovers all 5 source files and excludes `dist/bundle.js`; `ts-library` correctly discovers all 3 source files. `npx tsc --noEmit` (root and each fixture's own `tsconfig.json`) and `npm run lint` all pass clean.

**Next smallest task:** Session 4 — ts-morph project model extraction against local fixtures: imports/exports, internal dependency edges, entry-point heuristics, public surface, test relationships, React component detection.

### 2026-07-23 — Session 4 complete: JS/TS project model extraction

Added extraction modules under `src/analysis/js-ts/`, each producing pieces of the shared model directly (`@/lore/model`), rather than a separate per-language report format:

- `module-resolution.ts` — resolves relative (`./`, `../`) specifiers to a discovered source file; explicitly does not resolve tsconfig path aliases (ADR-0003 defers that to session 6) and exposes `looksLikePathAlias` so alias imports are surfaced as honest gaps rather than silently dropped.
- `imports.ts` — internal dependency edges (`Relationship`, kind `depends-on`) from both `import` declarations and re-export (`export ... from`) declarations. Unresolved relative imports produce an `unknown` gap; alias-looking imports produce an `unsupported` gap citing ADR-0003.
- `entry-points.ts` — `package.json` `main`/`module`/`bin` fields first (detected), then a React-DOM `render`/`createRoot` bootstrap-call heuristic (detected), then a conventional `src/index.ts(x)` fallback (inferred) only if nothing more specific was found.
- `public-surface.ts` — named exports (including re-exports) reachable from library/runtime entry points, via `SourceFile.getExportedDeclarations()`, which correctly resolves one level of re-export without a type-checked `Program`.
- `tests.ts` — test-to-implementation relationships: a test file's own relative import of its subject (detected) takes priority over filename convention (`foo.test.ts` -> `foo.ts`, inferred); orphaned test files produce an `unknown` gap.
- `react-components.ts` — JSX-returning functions (declarations, arrow functions, function expressions) and classes extending `Component`/`React.Component`/`PureComponent`, all `detected`.
- `extract-project.ts` — orchestrates the above into a `Project` (name, conservatively inferred `kind`, `languages`, `frameworks`) plus the relationships/entry points/public contracts/test relationships/gaps. Project-kind inference is a narrow, disclosed heuristic (bootstrap/CLI entry -> application; bare library manifest field -> library; otherwise left `unknown`) rather than a guess dressed up as fact.

Extended the fixtures to exercise more of this surface: `ts-react-app`'s `Footer` is now an arrow-function component and a new class component (`Greeting.tsx`, extends `Component`) was added, alongside the existing function-declaration component (`App`) — covering all three JSX-detection shapes. `App.tsx` imports `Greeting` via a relative path (resolves to a `detected` relationship) while `Header`/`Footer`/`format` stay behind the `@/*` alias (each correctly surfaces as an `unsupported` gap). `ts-library` gained `src/math.test.ts` (using Node's built-in `node:test`/`node:assert` so it type-checks standalone without adding a test-framework dependency), giving a `detected` test relationship via import.

Verified manually via `npm run extract -- <fixture-path>` (`scripts/extract-fixture.ts`, alongside the session 3 `discover` script): `ts-library` correctly extracts its `library` entry point, 3 internal dependency edges, 3 public contracts (`add`/`multiply`/`divide`), and 1 detected test relationship, with zero gaps. `ts-react-app` correctly infers `kind: "application"` from its bootstrap entry point, detects all 4 React components (one of each shape), extracts the one relative internal edge, and surfaces the 4 alias imports as `unsupported` gaps rather than silently missing them. Root `tsc --noEmit`, both fixtures' own `tsc --noEmit -p tsconfig.json`, and `npm run lint` all pass clean.

**Next smallest task:** Session 5 — derived views against JS/TS fixtures: Start Here (with rationale, evidence, and certainty per item), major areas, entry points, direct relationships — rendered on a plain unstyled page. This is the session that proves or disproves the core "wow moment" for JS/TS.

### 2026-07-23 — Session 5 complete: JS/TS derived views (Start Here, major areas)

Added `src/analysis/js-ts/derive-views.ts`, taking session 4's `JsTsExtraction` output (now also returning `sourceFilePaths`) and producing the shared-model `StructuralArea[]` and `Recommendation[]` (Start Here) views:

- **Major areas**: `docs/MODEL_OUTPUT.md`'s boundary-detection preference order is declared packages/workspaces first, framework convention next, then relationship-supported groupings, then a conservative structural fallback. Neither fixture declares a workspace or sub-package, so areas fall back to grouping source files by directory (one level under the project root, e.g. `src/components`), with that fallback made explicit in each area's own evidence/rationale rather than presented as a stronger conclusion. An area whose every file is a confidently-detected React component gets an additional `detected` evidence entry and a `responsibility` of "Presentational React components" — the one place a `responsibility` is set, since it's a direct generalization of already-detected per-file evidence rather than a guess. Area-to-area `directDependencyIds`/`directDependentIds` are derived from the file-level relationships already extracted in session 4. Gaps are attached to whichever area contains the file the gap's location points at.
- **Start Here**: seeds with what starts the system — an application's bootstrap/CLI entry point, or (when there is none) a library's declared public entry point — then follows the detected internal-dependency chain outward breadth-first, then pads with any major area not yet represented (largest first, for primary-area coverage), then closes with a representative test relationship when one exists. Order, evidence, and certainty are carried through from the underlying entry point/relationship/area/test data rather than reconstructed, so every item is directly traceable. This is a heuristic to validate against real repositories in session 6, not a fixed template — `docs/MODEL_OUTPUT.md` is explicit that the path should adapt to available evidence.

Added `scripts/derive-fixture.ts` (`npm run derive -- <fixture-path>`) alongside the existing `discover`/`extract` scripts, and a plain, unstyled page at `/fixtures/{name}` (`src/app/fixtures/[name]/page.tsx`, plus an index at `/fixtures`) rendering Start Here, major areas, entry points, direct relationships, test relationships, and gaps directly from the extraction + derivation — no styling, acquisition, or persistence, per the session's scope.

Verified manually: both fixtures produce well-evidenced, non-degenerate Start Here paths without any fixture-specific special-casing in the derivation logic itself —

- `ts-library` (a library): `src/index.ts` (declared entry, detected) → `src/math.ts` (detected, re-exported from the entry) → `src/internal/round.ts` (detected, math.ts's internal dependency) → `src/math.test.ts` (detected test relationship). All 4 items detected, 0 gaps — the "wow moment" holds cleanly when imports are relative.
- `ts-react-app` (an application): `src/index.tsx` (detected bootstrap entry) → `src/components` (inferred major area, all 3 files confidently-detected React components) → `src/utils` (inferred major area). Only 3 items and no dependency-chain items beyond the entry point, because `App.tsx`'s imports of `Header`/`Footer`/`format` all use the unresolved `@/*` alias (session 4's known, explicit gap) — an honest result, not a derivation bug, and it's the concrete signal that alias resolution (deferred to session 6) measurably shortens the JS/TS Start Here path today.

`npx tsc --noEmit`, `npm run lint`, and `npm run build` (including static generation of both `/fixtures/[name]` routes) all pass clean. Manually verified both rendered pages via `npm run dev` and `curl`.

**Next smallest task:** Session 6 — validate Start Here and major-area detection against 1–2 pinned real public JS/TS repos; adjust heuristics before moving to acquisition or Python. Given this session's finding, resolving `tsconfig.json` path aliases (currently an explicit `unsupported` gap, deferred by ADR-0003) is the most likely first heuristic adjustment session 6 will surface, since it directly limits the Start Here dependency chain on alias-heavy repositories like `ts-react-app`.

### 2026-07-23 — Session 6 in progress: validated and fixed against real repos (execa, globby)

Ran the extractor and derived views (no code changes yet at this point) against local clones of the two pinned repos, https://github.com/sindresorhus/globby (22 source files — a good manual-review size) and https://github.com/sindresorhus/execa (598, dominated by an unrecognized `test/` tree). This surfaced three real heuristic gaps, none of them in the session 5 derivation logic itself:

1. `entry-points.ts` only read `package.json`'s `main`/`module`/`bin` fields — not the modern `exports` field, which is what globby (and an increasing share of modern npm packages) declares instead. Result: zero entry points found, `project.kind` fell back to `"unknown"`, public-surface extraction found nothing (it requires an entry point), and Start Here had no seed to anchor on.
2. The conventional entry-point fallback only checked `index.ts`/`index.tsx` (and `src/` variants) — never `.js`/`.jsx` — so even a plain-JavaScript package with no manifest field would find nothing.
3. `isTestFile` (`tests.ts`) only recognized the `foo.test.ts` / `foo.spec.ts` suffix convention and `__tests__` directories — not mocha/tape/ava-style `test/foo.js` or `tests/foo.js` layouts. Against both real repos, this meant real test directories (and, for execa, `test/fixtures/*.js` support data spawned by tests) were indistinguishable from ordinary source, so they got graded as ordinary structural areas and, being the largest by file count, dominated Start Here's area-padding step — precisely the "list of the largest files" failure mode `docs/MODEL_OUTPUT.md` says Start Here must avoid.

Fixed all three: `entry-points.ts` now resolves `exports` (string form, a `"."` subpath entry, or a conditions object preferring `import`/`default` over `require` and skipping `types`) ahead of `main`/`module`, since `exports` takes precedence when present; the conventional fallback candidate list now includes `.js`/`.jsx`. `isTestFile` now also matches any file under a top-level `test/` or `tests/` directory. Added a separate, narrower `isTestSupportFile` concept in `derive-views.ts` for conventional non-code test-support directories (`fixtures/`, `__fixtures__/`, `__mocks__/`) — distinct from `isTestFile` since fixture data doesn't look like a test itself — and excluded both test-only and test-support-only areas from Start Here's area-padding step (they still appear in the Major Areas list, now labeled with a `responsibility` of `"Tests"` or `"Test fixtures/support data"` and matching evidence, rather than silently miscounted as ordinary implementation areas).

Re-verified after the fix:

- **globby**: Start Here is now `index.js` (detected, via the `exports` field) → `ignore.js` → `utilities.js` (both detected, direct dependencies) → `tests/convert-path-to-pattern.js` (detected test relationship) — 4 items, all detected, the `fixtures/*` and `tests/` directories no longer crowd out the real implementation.
- **execa**: Start Here is `index.js` (detected, `exports` field) → 5 of its direct `lib/` dependencies (all detected) → a real test file (detected) — 7 items, all detected, none of them from `test/`, `test-d/`, or `types/`.
- Both local fixtures (`ts-react-app`, `ts-library`) re-verified unchanged (no regression) — expected, since neither has an `exports` field, a plain-`.js` entry point, or a top-level `test`/`tests`/`fixtures` directory.
- `npx tsc --noEmit` and `npm run lint` pass clean on all of the above.

**Remaining open item surfaced by this same execa run, not yet fixed:** Major Areas is not filtered the way Start Here now is — execa's `lib/`, `test/`, `test-d/`, and `types/` trees each have a dozen-plus subdirectories, so the raw directory-grouping fallback produces **44 major areas**, nowhere near the "concise set of evidence-backed structural areas" `docs/MODEL_OUTPUT.md` calls for. Test-only and test-support-only areas are now at least labeled honestly (`responsibility: "Tests"` / `"Test fixtures/support data"`) rather than presented as ordinary implementation areas, but they still each count as a first-class Major Area. Fixing this well likely means either not listing pure test/test-support/types-only areas as Major Areas at all (folding their `testRelationshipIds` into the implementation area they test instead), or capping/collapsing sibling subdirectories under a shared parent when a directory has many children — a real design decision, not a one-line heuristic tweak, so it's deferred rather than improvised here.

### 2026-07-23 — Session 6 continued: re-pinned repos, two more real bugs found and fixed

The pinned repo list for this session was revised (this file, session 6 line) to `sindresorhus/globby`, `stemmlerjs/simple-typescript-starter`, and `pieces-app/example-typescript` — smaller, more conventional TypeScript repos than `execa`, better suited to manual review. `execa` remains a useful data point (previous entry) but is no longer one of the required pins.

Validated the two new pins against local clones and found two more real gaps:

1. **`simple-typescript-starter`** surfaced a minor one: its root `jest.config.js` was discovered as ordinary source (nothing in `discovery.ts` excludes config files by name, only by directory), became its own single-file "major area" at `.`, and padded into Start Here as noise ahead of nothing better. Fixed narrowly: added `isToolingConfigFile` to `derive-views.ts` — a root-level, known-tool-name allowlist (`jest`, `webpack`, `babel`, `vite`, `next`, `eslint`, etc., matched against `*.config.*`/`*.setup.*`), not a generic `*.config.*` pattern. The generic pattern was deliberately rejected: `pieces-app/example-typescript` (the next repo tried) has `src/platform.config.ts`, real, actively-imported application code (5 importers) that a generic pattern would have wrongly hidden. Areas that are all-tooling-config now get `responsibility: "Build/tooling configuration"` and matching evidence, and are excluded from Start Here's area-padding step alongside the existing test/test-support exclusion (all three now tracked via one `NON_PRIMARY_AREA_RESPONSIBILITIES` set).

2. **`pieces-app/example-typescript`** surfaced the most serious bug of this session: `entry-points.ts`'s `callsReactDomRender` bootstrap heuristic matched any call expression whose callee text ended in `render` — including a bare, unqualified `render(...)` call. React Testing Library's `render(<Button />)` (used in essentially every React component test) matches that exactly, so both of this repo's test files (`button.test.tsx`, `textinput.test.tsx`) were misclassified as `detected` **application bootstrap entry points**, ranking ahead of the repository's real entry point (`src/index.tsx`, which itself calls `createRoot(rootElement).render(...)`) in Start Here's seed selection. Fixed by requiring a property-access call (`/\.render$/`, dropping the bare-name branch entirely) — genuine ReactDOM bootstrap code always calls render on the `ReactDOM` import or a root/container object, never a standalone function literally named `render`, so this loses no true positives. Added a second, defense-in-depth layer: the bootstrap-heuristic scan now also skips any file `isTestFile` recognizes, regardless of what it matches.

Re-verified after both fixes:

- **`example-typescript`**: Start Here is now `src/index.tsx` (detected, `main` field) → `src/app/App.tsx` → 4 of the components it directly imports (all detected) → a real, detected test relationship — 7 items, all detected, no test file anywhere in the seed position. `__mocks__/` (a root-level test-support directory) and `.` (root, containing only `jest.config.js`) are correctly excluded from Start Here and labeled `"Test fixtures/support data"` / `"Build/tooling configuration"` in Major Areas.
- **`simple-typescript-starter`**: Start Here is `src/index.ts` (inferred, conventional fallback — this repo's `main` field points at compiled `build/index.js`, correctly unresolved since `build/` is excluded from discovery) → `src/useCase` (inferred major area) → `src/index.spec.ts` (inferred test, filename convention — its content is an unrelated dummy test, an honest limitation of a naming-convention-only heuristic, not a bug). `jest.config.js` no longer appears in Start Here.
- **`globby`**: unchanged and still correct (re-verified, no regression from either fix).
- Both local fixtures (`ts-react-app`, `ts-library`) re-verified unchanged — neither has a `render(...)`-calling test file or a root-level tooling-config file.
- `npx tsc --noEmit` and `npm run lint` (now `eslint --fix`, plus the project's new `prettier`/`husky`/`lint-staged` setup) pass clean on all of the above.

All three currently-pinned repos (globby, simple-typescript-starter, example-typescript) now produce well-evidenced, non-degenerate Start Here paths with no test files or config files in seed position. The Major Areas conciseness item (44 areas on execa) from the previous entry remains open and deferred, as decided — it wasn't re-triggered by any of the three currently-pinned (smaller) repos, since none has execa's depth of sibling subdirectories.

**Next smallest task:** Session 6 is now in good shape against all three pinned repos; remaining before moving to session 7 (GitHub API/acquisition) is a decision on the Major Areas conciseness item, and optionally resolving `tsconfig.json` path aliases (still an explicit `unsupported` gap affecting `ts-react-app`'s Start Here specifically, not yet observed to matter on a real pinned repo).

### 2026-07-23 — Session 6 continued: fourth pinned repo (react-weather-app), one more real fix

Added a fourth pin, `Adedoyin-Emmanuel/react-weather-app` (this file, session 6 line) — a real, messy, un-maintained-looking Create React App project (plain JavaScript/JSX, no TypeScript, 32 source files, no `main`/`exports` field, bootstrap detected only via the `createRoot(...).render(...)` heuristic). Useful specifically because it's the kind of repo Repo Lore's actual users will submit — not a curated example.

This confirmed the `render()` fix from the previous entry generalizes correctly: `src/App.test.js` calls `render(<App />)` (React Testing Library, bare call) and was correctly **not** misclassified as bootstrap, while `src/index.js`'s real `ReactDOM.createRoot(...).render(...)` (a property-access call) was correctly detected.

It also surfaced one more real gap, independent of entry-point detection: relative imports of static assets (`.svg`, `.png`, `.gif`, `.css`, ...) — completely ordinary in a bundler-based (webpack/CRA) app — don't resolve to a JS/TS source file, and were logged as `unknown` "could not resolve" gaps. This produced **68 gaps for one repo**, none of them a real analysis limitation: bundlers resolve these paths, not the JS/TS module graph, so failing to find a source file at that path isn't uncertainty worth surfacing — it's out of scope by design, the same way a bare external-package specifier (`"react"`) is already intentionally not recorded.

Fixed with `looksLikeAssetImport` in `module-resolution.ts` (an extension allowlist: `.css`/`.scss`/`.sass`/`.less`, common image formats, common font formats, common media formats), checked in `imports.ts` before attempting resolution — a relative specifier matching it is skipped entirely, exactly like an external bare specifier, rather than resolved-and-failed-and-gapped.

Re-verified: `react-weather-app`'s gap count went from 68 to **0**; Start Here is now `src/index.js` (detected bootstrap) → `src/App.js` → 2 of its direct dependencies → 2 of `App.js`'s page dependencies → `src/App.test.js` (detected test relationship) — 7 items, all `detected`. Major Areas came out to a clean, concise 6, with `src/components` and `src/pages` both correctly labeled `"Presentational React components"` (every file in each is a confidently-detected React component). Both local fixtures and all three previously-pinned real repos re-verified unchanged (no regression). `npx tsc --noEmit` and `npm run lint` pass clean.

**Next smallest task:** unchanged from the previous entry — decide on the Major Areas conciseness item (44 areas on execa; not re-triggered by any of the four now-pinned repos) before moving to session 7. No other gaps found across four real repos and two fixtures in this pass.

**Decision (2026-07-23):** the Major Areas conciseness item is explicitly deferred, not resolved — it remains open in this log and is re-opened whenever a repo with execa-like directory depth is encountered (or by session 15's fixture work). Moving on to session 7 as scoped.

### 2026-07-23 — Session 7 complete: GitHub URL input, validation/normalization, GitHub API client

Added `src/github/`, the first code under the app that isn't the JS/TS analyzer:

- `parse-repo-url.ts` — `parseGitHubRepoUrl(input)`, a pure `{ ok: true, value: { owner, repo } } | { ok: false, reason }` normalizer. Accepts what people actually paste (missing scheme, `www.`, trailing slash, `.git` suffix, extra path segments like `/tree/main`); rejects non-github.com hosts, missing owner/repo segments, and invalid owner/repo names with a specific, honest reason each time rather than a generic "invalid URL." Ref/path suffixes are intentionally ignored — the MVP always analyzes the resolved default branch's HEAD, never a user-specified ref.
- `client.ts` — `resolveRepositoryHead(owner, repo)`, calling `GET /repos/{owner}/{repo}` (for `default_branch`) then `GET /repos/{owner}/{repo}/commits/{default_branch}` (for the HEAD SHA). Requires `GITHUB_TOKEN` (throws a typed `GitHubApiError` with `code: 'missing-token'` if unset, per ADR-0002's consequence that authenticated access is required once acquisition code is built — no silent unauthenticated fallback). Maps GitHub's response codes to typed error codes (`not-found`, `unauthorized`, `rate-limited`, `unknown`) with a human-readable message each, so callers can produce an honest error instead of a generic failure (acceptance criterion 2).

Wired both into the home page (`src/app/page.tsx`, now a real form replacing the `create-next-app` placeholder content) via a Server Action (`src/app/actions.ts`, `resolveRepository`) and `useActionState`: paste a URL, submit, and see either a specific validation/API error or the resolved owner/repo/default branch/HEAD SHA. This is intentionally not `/lore/{owner}/{repo}` yet — that page renders persisted analysis (session 10), and no acquisition/analysis pipeline is wired to a fetched repo yet (session 8), so showing only the resolution result keeps the page honest about what's actually been built. Kept plain/unstyled, matching the fixtures pages' precedent; removed the unused `page.module.css` and the stock `create-next-app` metadata/content. Added `GITHUB_TOKEN` to `.env.example` with setup notes (no scopes needed for public repos).

Verified manually (no test suite yet — session 15): a standalone script exercised `parseGitHubRepoUrl` against 11 cases (valid full/bare/trailing-slash/`.git`/extra-path URLs; invalid host, missing repo segment, malformed input, empty input, invalid characters) — all passed. A second script exercised `resolveRepositoryHead` against a real token (`gh auth token`, not persisted anywhere) for both a real public repo (`sindresorhus/globby`, correctly resolved `main`/HEAD SHA) and a nonexistent repo (correctly threw `not-found`), plus a missing-token run (correctly threw `missing-token`). Started `npm run dev` and drove the actual form's Server Action end to end with real multipart POSTs: a valid URL rendered the resolved owner/repo/branch/SHA, an invalid URL and a non-GitHub host each rendered their specific validation reason, and a nonexistent repo rendered the specific "not found — it may not exist, or it may be private" message. `npx tsc --noEmit`, `npm run lint`, and `npm run build` (including static generation) all pass clean.

**Next smallest task:** Session 8 — tarball fetch + safe extraction (size/file-count/time limits, path sanitization) into a temp dir, with cleanup; swap the JS/TS analyzer's input from local fixtures to a fetched repo.

### 2026-07-23 — Session 8 complete: tarball fetch + safe extraction, analyzer swapped to a fetched repo

Added `src/acquisition/`, implementing the extraction safety limits ADR-0002 treats as part of the acquisition decision itself, on top of a real (`tar` npm package) tar/gzip implementation rather than a hand-rolled parser:

- `errors.ts` — `AcquisitionError` with typed codes (`github-error`, `tarball-too-large`, `extracted-too-large`, `too-many-files`, `unsafe-entry`, `timeout`), one per honest failure reason.
- `extract-tarball.ts` — `acquireTarballSource(tarballBody, limits?, signal?)`: streams the tarball response to a temp file with a hard cap on compressed bytes enforced as they arrive (GitHub doesn't reliably send a usable `Content-Length`), then extracts with `tar.x`, `strip: 1` (drops GitHub's wrapping `{owner}-{repo}-{sha}/` directory). The extraction `filter` rejects any entry that isn't a plain file or directory (no symlinks/hardlinks/device files — a known tar-based path-escape vector) and any entry whose resolved path falls outside the destination directory (defense in depth on top of node-tar's own path sanitization), and enforces hard caps on file count and total extracted size. Because node-tar's `filter` isn't documented as safe to throw from mid-stream, a violation is recorded and every remaining entry is skipped rather than raised immediately; the caller checks the recorded violation once extraction finishes and fails the whole acquisition — deliberately not a partial extraction, since a partial source tree would mean a partial, misleading analysis. `DEFAULT_EXTRACTION_LIMITS` (50MB compressed download / 300MB extracted / 20,000 files) are disclosed as tunable, reversible defaults, not a load-bearing design decision.
- `fetch-repo-source.ts` — `acquireRepositorySource(owner, repo)`: the single orchestration entry point, chaining session 7's `resolveRepositoryHead` with the new `fetchRepositoryTarball` (`src/github/client.ts`) and `acquireTarballSource`, wrapped in a wall-clock timeout (default 30s) via `AbortController`. The timeout is scoped to the tarball download specifically (network-bound and attacker-paceable) rather than the whole operation, since node-tar's synchronous local extraction doesn't support cooperative cancellation via `AbortSignal` — disclosed in a code comment as a deliberate scope decision, not an oversight. Always returns a `cleanup()` that removes the entire temp work directory, called both by the orchestrator on any failure and expected from every caller on success.

Added `scripts/analyze-github-repo.ts` (`npm run analyze-repo -- <github-url>`, loading `.env.local` via `tsx --env-file-if-exists`) proving the session's actual goal — the existing JS/TS analyzer (`extractJsTsProject`/`deriveJsTsViews`) runs _unmodified_ against a directory produced by `acquireRepositorySource`, exactly as it already does against local fixtures.

Verified manually (no test suite yet — session 15):

- `npm run analyze-repo -- https://github.com/sindresorhus/globby` reproduces exactly the Start Here / major-area result already validated against a local clone in session 6 (`index.js` → `ignore.js`/`utilities.js` → a detected test relationship; 6 major areas correctly labeled `fixtures/*`/`tests` as non-primary), fetched live from GitHub end to end, with the temp directory confirmed removed afterward.
- A nonexistent repository produces the same honest `not-found` message as session 7's resolution step (acquisition fails before any tarball fetch is attempted).
- A standalone script built adversarial tarballs by hand (`tar.c` over a staged directory) and exercised `acquireTarballSource` directly: a symlink entry pointing outside the extraction directory is rejected (`unsafe-entry`), a file count over a lowered cap is rejected (`too-many-files`), an extracted size over a lowered cap is rejected (`extracted-too-large`), and a normal small tarball extracts correctly and is fully cleaned up afterward. All five cases passed. A full automated adversarial fixture (oversized files, deep nesting, a committed tar path-traversal attempt) remains session 15's job, per acceptance criterion 13 and the session 15 bullet — this session's manual checks are a sanity check on the same code paths, not a substitute for it.

`npx tsc --noEmit`, `npm run lint`, `npm run build`, and `prettier --check` all pass clean.

**Next smallest task:** Session 9 — DB schema + migrations: `repos`, `analysis_runs` (local Postgres or SQLite is enough; managed provider not needed yet).

### 2026-07-23 — Session 9 complete: DB schema + migrations (Postgres)

**Decision:** the plan left the engine choice open ("local Postgres or SQLite is enough"). Chose local Postgres over SQLite: production (ADR-0004, ADR-0005) is written assuming Postgres — `analysis_jobs`' future `SELECT ... FOR UPDATE SKIP LOCKED` claim and `analysis_runs.result` as JSONB both assume it — so building the real schema now avoids a dialect rewrite at deploy time (session 17), a more costly reversal than the extra local setup step. Docker was available, so the setup cost is one `docker compose up -d db`.

Added:

- `docker-compose.yml` — a single `db` (`postgres:17-alpine`) service with a named volume, credentials matching `.env.example`'s `DATABASE_URL`.
- `src/db/migrations/0001_init.sql` — hand-written SQL, not a migration framework (node-pg-migrate/Prisma/Drizzle): two tables don't yet justify one, and this is a reversible, low-dependency choice consistent with the project's existing scripts-over-frameworks pattern. `repos (id, owner, name, created_at)`, unique on `(owner, name)`. `analysis_runs (id, repo_id, commit_sha, analyzer_version, status, result jsonb, error_message, created_at)`, unique on `(repo_id, commit_sha, analyzer_version)` — ADR-0005's idempotency key, enforced by the database itself, not just application logic. `result` stores the full serialized `Lore` (`src/lore/model.ts`) rather than normalizing every nested entity (structural areas, relationships, evidence, ...) into its own table: an MVP that renders one immutable, read-mostly document per run doesn't need that relational complexity yet, and it's a reversible decision — materialized columns/indexes on `jsonb` paths can be added later without changing the key. `status` intentionally matches `AnalysisSnapshot['status']` (`completed`/`partial`/`failed`, session 2) exactly rather than ADR-0004's older "queued/running/done/failed" language: since analysis runs synchronously and in-process, a row is only ever inserted once a result or failure already exists, so no persisted "running" state is needed yet — that gap is reopened only if session 16's async dispatch needs one. A partial index on `(repo_id, created_at DESC) WHERE status IN ('completed','partial')` supports acceptance criterion 10's "render the latest completed run" lookup directly.
- `src/db/client.ts` — `getDbPool()`, a lazily-created singleton `pg.Pool` reading `DATABASE_URL`, shared by the migration runner now and application persistence code from session 10 on.
- `scripts/migrate.ts` (`npm run migrate`) — applies pending `.sql` files in filename order inside a transaction each, tracking applied filenames in a `schema_migrations` table; re-running is a no-op.
- `package.json` — `db:up`/`db:down` (`docker compose up -d db` / `down`), `migrate`; added `pg` + `@types/pg`.
- `.env.example`/`.env.local` — added `DATABASE_URL`.

This session is schema/migrations only, per its scope — no application code reads or writes these tables yet (session 10).

Verified manually (no test suite yet — session 15): started the local Postgres container, ran `npm run migrate` (applied `0001_init.sql`; confirmed re-running is a no-op), then exercised the constraints directly against the running database — duplicate `(owner, name)` rejected, duplicate `(repo_id, commit_sha, analyzer_version)` rejected (ADR-0005 idempotency), an invalid `status` value rejected by the check constraint, a `failed` run with `result = NULL` and an `error_message` insert succeeded, `EXPLAIN` confirmed the partial index is used for the latest-active-run query shape, and deleting a `repos` row cascade-deleted its `analysis_runs`. Reset both tables to empty afterward. `npx tsc --noEmit`, `npm run lint`, and `prettier --check` all pass clean.

**Next smallest task:** Session 10 — persist `analysis_run`, idempotent on `(owner, repo, commit_sha, analyzer_version)`; render `/lore/{owner}/{repo}` from persisted JS/TS results with evidence links and certainty labels.

### 2026-07-23 — Session 10 complete: persistence + `/lore/{owner}/{repo}`, first full end-to-end slice for JS/TS

Added the pieces connecting everything built in sessions 2–9 into one path: submit a URL, get a persisted, evidence-backed Lore at a stable URL.

- `src/lore/build-lore.ts` — `buildJsTsLore(...)` assembles a full `Lore` (session 2's model) from a JS/TS extraction + derived views + repository/commit identity. `snapshot.status` is computed by actually calling `evaluateMinimumValueContract` on the assembled Lore, not set to `'completed'` just because extraction didn't throw — a project that extracts but doesn't meet the minimum value contract is honestly `'partial'`.
- `src/analysis/js-ts/version.ts` — `JS_TS_ANALYZER_VERSION` (ADR-0005's key component, bumped when extraction/derivation logic changes in a way that should invalidate prior conclusions).
- `src/db/repos.ts`, `src/db/analysis-runs.ts` — `upsertRepo`; `saveAnalysisRun` (idempotent insert via `ON CONFLICT (repo_id, commit_sha, analyzer_version) DO NOTHING`, falling back to fetching the existing row — never a duplicate, never silently overwritten); `getAnalysisRunByKey` (exact-key lookup, any status, for the idempotency short-circuit); `getLatestAnalysisRunForRepo` (a repo's single most recent run regardless of status — see the migration note below for why unfiltered).
- `src/analysis/analyze-and-persist.ts` — `analyzeAndPersistRepository(owner, repo)`, the orchestration entry point: upsert the repo, resolve HEAD, short-circuit if that exact commit+analyzer-version was already run (avoids redundant tarball fetch/extraction on repeat submissions — verified below), otherwise acquire → extract → derive → persist. A thrown error from acquisition or extraction is caught and persisted as an honest `'failed'` run with `error_message`, not left unrecorded; only a resolution failure (bad URL target, repo doesn't exist) — which has no commit to key a run on — propagates to the caller instead.
- `src/db/migrations/0002_analysis_runs_latest_run_idx.sql` — corrects session 9's indexing choice: `/lore/{owner}/{repo}` needs the latest run _regardless of status_ (a repo whose latest attempt failed must still render that failure, not silently fall through to "never analyzed"), which a partial `WHERE status IN (...)` index can't serve. Drops session 9's partial index, adds a full `(repo_id, created_at DESC)` index. Immutable-log note: this corrects rather than edits session 9's entry above, matching this project's own append-only philosophy for analysis history.
- `src/components/LoreView.tsx` — one shared, plain/unstyled `Lore` renderer used by both the real `/lore/{owner}/{repo}` page and `/fixtures/{name}` (which now also builds a real `Lore` via `buildJsTsLore` instead of rendering raw extraction pieces ad hoc), so the two can't drift apart. Takes optional `sourceUrl`/`areaUrl` callbacks for linking claims back to GitHub; omitted entirely for fixtures (no real commit to link to), so locations render as plain `<code>` rather than a fabricated link. Certainty labels are capitalized (`Detected`/`Inferred`/`Unknown`/`Unsupported`) per the product's own vocabulary.
- `src/github/urls.ts` — `githubBlobUrl` (files, `/blob/{sha}/{path}`) and `githubTreeUrl` (directories, `/tree/{sha}/{path}`, root `.` omits the path segment). Initially used `githubBlobUrl` everywhere; manual verification caught that `StructuralArea.location` and some Start Here items (derive-views' area-padding step) point at _directories_, not files, which `LoreView` now distinguishes by checking each location against the Lore's own `structuralAreas` rather than guessing from the path shape.
- `src/app/lore/[owner]/[repo]/page.tsx` — reads the latest run and renders it via `LoreView`; a `'failed'` run (or one with no `result`) renders its `error_message` honestly instead of a generic 404.
- `src/app/actions.ts`/`src/app/page.tsx` — the Server Action now calls `analyzeAndPersistRepository` and `redirect()`s to `/lore/{owner}/{repo}` on any outcome that produced a run (including `'failed'`); only a pre-run failure (invalid URL, repo doesn't exist) stays on the home page as an inline error, since `redirect()` must be called outside the `try`/`catch` or Next.js's internal redirect signal gets swallowed by the `catch`.
- `src/github/client.ts` — `resolveRepositoryHead` now also returns the repository's GitHub `description` (already present in the same API response session 7 was already fetching), surfaced on the Lore page rather than left unused.

**A real bug found via this session's own end-to-end run, fixed in `extract-project.ts`:** `inferProjectKind`'s evidence and the React-component-detection evidence never set a `location` — invisible before this session because `evaluateMinimumValueContract` (built in session 2) had never actually been run against real extraction output; every prior session only manually inspected raw fields. This silently forced every real repository's `snapshot.status` to `'partial'` (evidence without a location fails `hasTraceableEvidence`) even when the analysis was otherwise complete. Fixed by citing the specific entry point that justified the kind inference, and the first detected component as a representative citation for the framework-detection claim.

Verified manually end to end (no test suite yet — session 15) with the local Postgres container running and a real POST to the running dev server's Server Action:

- `https://github.com/sindresorhus/globby` → `303` redirect to `/lore/sindresorhus/globby` → renders with `Status: completed`, a `Start Here` path whose items link to real `github.com/sindresorhus/globby/blob/14f4f47.../...` URLs, and Major Areas whose directory rows link to `.../tree/14f4f47.../...` (root `.` area correctly links to the tree root, not a literal `/tree/{sha}/.`).
- Idempotency: resubmitting the identical URL returned the same `/lore/...` redirect in **594ms** vs. **1845ms** for the first (real) analysis — confirmed via the dev server's own request-timing log — and the database still held exactly **one** `analysis_runs` row afterward (`getAnalysisRunByKey` short-circuit confirmed working, not just the unique constraint silently preventing a duplicate).
- A nonexistent repository produced the same honest inline error as sessions 7–8, with **no** row written (resolution fails before any commit exists to key a run on).
- `fixtures/ts-library` and `fixtures/ts-react-app` re-verified against the refactored `LoreView`/`buildJsTsLore` path: `ts-library` renders `Status: completed`; `ts-react-app`'s directory-location Start Here items (`src/components`, `src/utils`) correctly render as plain text (no `sourceUrl` passed for fixtures, so no fabricated link).
- An unanalyzed `owner/repo` correctly 404s at `/lore/{owner}/{repo}`.

Reset both tables to empty afterward. `npx tsc --noEmit`, `npm run lint`, `npm run build` (including static generation of both fixture routes), and `prettier --check` all pass clean.

**Next smallest task:** Session 11 — manual re-analysis action (rate-limited) + honest unsupported/failed states. The failed-run rendering path already exists (this session); session 11's job is the rate-limited manual trigger and any UI for it.

### 2026-07-23 — Session 11 complete: rate-limited manual re-analysis action

Added the pieces for acceptance criterion 11. The failed-run rendering path (session 10) already covered the "honest failed state" half of this session's checklist item; true "unsupported project type" detection remains correctly out of scope until Python analysis exists (session 12+) to know what "unsupported" actually means — a repo with zero JS/TS files today already renders honestly as `partial` with empty sections (no fabrication), just not yet labeled "unsupported" specifically.

- `src/db/migrations/0003_repos_reanalysis_rate_limit.sql` — adds `repos.last_analysis_requested_at`.
- `src/analysis/reanalysis-rate-limit.ts` — `claimReanalysisAttempt(repoId)`, a single atomic `UPDATE ... WHERE ... RETURNING` claim (`REANALYSIS_COOLDOWN_SECONDS = 60`, a disclosed/tunable default matching the precedent set by session 8's `DEFAULT_EXTRACTION_LIMITS`) throwing a typed `RateLimitedError` (with `retryAfterSeconds`) if claimed too recently. Gates the attempt _before_ `resolveRepositoryHead`'s GitHub API calls, not just the tarball fetch/extraction ADR-0005's idempotency already skips at an unchanged commit — a scripted rapid click could otherwise burn the shared token's rate limit even when every call short-circuits before touching the tarball.
- `src/analysis/analyze-and-persist.ts` — `analyzeAndPersistRepository` now calls `claimReanalysisAttempt` right after `upsertRepo`, so both the home-page submission and this session's new re-analyze action share one rate-limited choke point rather than two divergent implementations.
- `src/app/actions.ts` — added `reanalyzeRepository(owner, repo, prevState, formData)`, a Server Action bound to `owner`/`repo` via `.bind()` (the button has no form fields to carry them). Compares the latest run's id before and after the call to report honestly whether a new commit was actually found and analyzed, or the attempt short-circuited on an unchanged commit — a plain "done" could otherwise look like a no-op silently succeeded. `resolveRepository` also now catches `RateLimitedError` alongside `GitHubApiError`.
- `src/components/ReanalyzeButton.tsx` (client) — the button itself, via `useActionState`, matching the home page's existing pending/error-state pattern.
- `src/components/LoreView.tsx` — added an optional `actions` prop, rendered next to the analysis-metadata list, so the real `/lore/{owner}/{repo}` page can inject the re-analyze button without `/fixtures/{name}` (which has no real repository to re-analyze) needing to know about it.
- `src/app/lore/[owner]/[repo]/page.tsx` — renders `<ReanalyzeButton>` in both the failed-run branch and the normal `LoreView` branch, via the new `actions` prop.

Verified manually (no test suite yet — session 15), with the local Postgres container and dev server running:

- `claimReanalysisAttempt` exercised directly: a first claim on a repo with a `NULL` timestamp succeeds; an immediate second claim throws `RateLimitedError` with `retryAfterSeconds` in `(0, 60]`; after manually backdating the timestamp past the cooldown, a claim succeeds again.
- `analyzeAndPersistRepository` exercised directly against the already-analyzed `pieces-app/example-typescript` (unchanged commit): the first call returned the same run id as the existing latest run (honest no-op, no redundant tarball fetch), and an immediate second call threw `RateLimitedError`.
- Confirmed via `curl` that the "Re-analyze" button renders on both a `completed` run's page (`pieces-app/example-typescript`) and a `failed` run's page (`react/react`).
- `npx tsc --noEmit`, `npm run lint`, `npm run build` (including static generation of both fixture routes), and `prettier --check` all pass clean.

**Next smallest task:** Session 12 — write an ADR for the Python analysis approach (library and syntactic-vs-typed tradeoff, mirroring ADR-0003), then build the Python extractor against the same shared Lore-model contract.

### 2026-07-27 — Session 12 complete: ADR-0006 (tree-sitter-python) + Python extractor

**Decision (ADR-0006):** Python source is parsed with **tree-sitter-python via `web-tree-sitter`**, syntactic-only — the direct Python analogue of ADR-0003's ts-morph decision. Confirmed before committing that `tree-sitter-python`'s npm package ships a prebuilt `tree-sitter-python.wasm` directly in its tarball, so `web-tree-sitter` (pure WASM, no native compilation) needs no system Python interpreter and no `node-gyp` build step — it stays inside the single Next.js deployable (ADR-0001). Shelling out to system `python3`+`ast` was rejected as a permanent second-language-runtime operational dependency for a solo maintainer; a regex-based scanner was rejected as too fragile for public-surface/test detection. Added `web-tree-sitter`, `tree-sitter-python` (parsing), `smol-toml` (pyproject.toml), and `ini` (setup.cfg) as dependencies.

Built `fixtures/python-app/` (`pyproject.toml` with a `[project.scripts]` console entry, `src/pyapp/` with a bootstrap chain `__main__.py` → `cli.py` → `core.py` → `utils.py`, `tests/test_core.py`, and a `.venv/lib/.../site-packages/somepkg/__init__.py` stub to verify exclusion) and `fixtures/python-library/` (`setup.py` + `setup.cfg`, `src/pylib/` with a public surface via `__init__.py`'s `__all__`, an internal `_internal/rounding.py` dependency, a `setup.py`-declared console script, `tests/test_core.py` (pytest) and `tests/test_legacy.py` (`unittest.TestCase`)).

Added `src/analysis/python/`, mirroring `src/analysis/js-ts/`'s module shape:

- `parser.ts` — `createPythonParser()`/`parsePythonSource()`: one-time `Parser.init()` plus loading the grammar from `tree-sitter-python`'s own package directory (`require.resolve('tree-sitter-python/package.json')` + `tree-sitter-python.wasm`).
- `discovery.ts` — walks the directory tree for `.py` files, excluding `__pycache__`, `.venv`/`venv`/`env`, `.tox`, `.eggs`, `*.egg-info`, `build`, `dist`, `site-packages`, `.git`, tool caches, parsing each into a tree-sitter tree.
- `syntax.ts` — shared, generic tree-walking helpers (`descendantsOfType`, `directChildrenOfType`, `isTopLevel`, string-literal helpers) used across the other modules.
- `module-resolution.ts` — resolves absolute dotted imports (`import foo.bar`) against detected source roots (`src/` when present, else repo root) and relative imports (`from .foo import bar`, dot-count based) against the importing file's own package directory.
- `imports.ts` — walks `import_statement`/`import_from_statement` nodes into `depends-on` relationships. Deliberately asymmetric with JS/TS: an **absolute** import that doesn't resolve is silently treated as external (stdlib/third-party) and is not a gap, since `import os` and an unresolved internal import are syntactically indistinguishable; a **relative** import that doesn't resolve unambiguously is intra-project and does produce an `unknown` gap. Also exports `resolvedDependenciesByFile` (each file's deduped resolved targets) so `tests.ts` can reuse the already-resolved import graph instead of re-parsing.
- `project-config.ts` — reads `pyproject.toml` (`smol-toml`: `project.scripts`/`project.gui-scripts`/`tool.poetry.scripts`, project name), `setup.cfg` (`ini`: `[metadata] name` only — its `[options.entry_points]` `console_scripts` uses a configparser-specific multi-line continuation format the `ini` package doesn't parse; a disclosed, not-yet-supported case), and `setup.py` (tree-sitter walk of a top-level `setup(...)` call's `name`/`entry_points` keyword arguments — never executed, consistent with treating repository content as untrusted).
- `entry-points.ts` — declared console scripts first (detected), then the `__main__.py` convention (detected), then a top-level `if __name__ == "__main__":` guard (detected, test files defensively excluded from the start — the exact React-Testing-Library-style misclassification found the hard way in JS/TS session 6 was designed around up front here, since `test_legacy.py`'s `unittest.main()` guard is exactly that shape), then the declared package name resolving to a package's `__init__.py` as its library entry, then a conventional `main.py` fallback.
- `public-surface.ts` — a library entry's `__init__.py` `__all__` (detected) when present, else non-underscore top-level definitions and re-export imports (inferred).
- `tests.ts` — `isTestFile` (pytest `test_*.py`/`*_test.py` or a top-level `test`/`tests` directory; `unittest.TestCase` suites are covered by the same convention, not detected separately) plus import-based (detected) or filename-convention (inferred) test-to-implementation linking, reusing `imports.ts`'s resolved dependency map.
- `extract-project.ts` — orchestrates the above into a `Project` + relationships/entry points/public contracts/test relationships/gaps, matching `JsTsExtraction`'s shape exactly. `version.ts` adds `PYTHON_ANALYZER_VERSION`.

**A real bug found and fixed during this session's own verification:** `web-tree-sitter` constructs a fresh Node wrapper object on every `.parent`/`.child()`/`.rootNode` access, so `===` between two independently-obtained wrappers for the same underlying node is always `false` — not documented anywhere obvious, found by noticing `public-surface.ts` was silently returning zero contracts for `fixtures/python-library`'s `__init__.py` despite a clearly-present `__all__`. Confirmed via a scratch script that `tree.rootNode === tree.rootNode` is `false` but `.equals()` (and a stable numeric `.id`) work correctly. Fixed by adding `nodesEqual()` (using `.equals()`) to `syntax.ts` and auditing every Python module for `===`/`!==` comparisons between tree-sitter nodes — only `isTopLevel` and the `__all__` assignment's statement-parent check were affected; every other structural comparison already worked off of node `.type`/`.text` (plain strings, unaffected by wrapper identity).

**Disclosed, deferred (not bugs):** (1) `setup.cfg`'s multi-line `console_scripts` format isn't parsed (see `project-config.ts` above). (2) `fixtures/python-library` — a library — declares one console script (added deliberately, to get real fixture coverage of `setup.py`'s `entry_points` parsing) and consequently gets `project.kind: "application"` from `inferProjectKind`, since that heuristic prioritizes any bootstrap/cli entry point over a library entry, mirroring JS/TS's existing (unchanged) precedent exactly — not a Python-specific defect, but worth re-examining in session 13 if it produces a misleading result against real repositories the way JS/TS's entry-point heuristics needed adjustment in session 6.

Verified manually (no test suite yet — session 15): `npm run extract-python -- fixtures/python-app` and `fixtures/python-library` (new `scripts/extract-python-fixture.ts`, mirroring the JS/TS `extract` script) — `python-app` correctly discovers 6 source files (excluding the `.venv/site-packages` stub), detects 3 entry points (console-script `cli`, `__main__.py`-convention `cli`, package `library`), 4 detected internal relationships, 1 detected test relationship, 0 gaps. `python-library` correctly discovers 8 source files, detects 2 entry points (`setup.py` console-script `cli`, package `library`), 5 detected internal relationships (including the `_internal/rounding.py` edge and both test files), 2 detected public contracts (`add`, `multiply` via `__all__`), 2 detected test relationships (one pytest, one `unittest.TestCase`), 0 gaps. `npx tsc --noEmit`, `npm run lint`, `npm run build`, and `prettier --check` all pass clean.

**Next smallest task:** Session 13 — validate Python Start Here and major-area output against the same usefulness threshold as JS/TS, using local fixtures and 1–2 pinned real public Python repos. Since no Python derive-views logic exists yet (session 5's JS/TS equivalent), this session needs to build it first — likely by generalizing `src/analysis/js-ts/derive-views.ts`'s area-grouping/Start-Here-assembly logic to operate on the shared model directly (it's already fairly language-neutral aside from the React-specific area label) rather than writing a second, parallel implementation. The `project.kind` heuristic note above and the `setup.cfg` multi-line entry-points gap are both worth revisiting if real-repo validation surfaces them as actual problems.

### 2026-07-27 — Session 13 complete: Python derive-views + real-repo validation

**Correction to this log's own recurring "no automated test suite yet — session 15" phrasing (sessions 3–12):** that claim was stale/inaccurate — `npm run test` (jest) already runs and gates every session's work, one spec file per extractor module, in both `src/analysis/js-ts/__tests__/` and `src/analysis/python/__tests__/` (42 suites / 298 tests passing at the start of this session). Session 15 remains the session for cross-fixture/adversarial integration tests, but per-module unit coverage has been the real, working convention all along; this session's new modules follow it rather than deferring.

**Generalized `derive-views.ts` rather than writing a second implementation**, per this file's own prior note: extracted the ~95%-already-language-neutral core of `src/analysis/js-ts/derive-views.ts` into `src/analysis/shared/derive-views.ts` (`deriveViews(input, config)`), parameterized by `isTestFile`/`isToolingConfigFile` and an optional `detectSpecialAreaResponsibility(files, areaName)` hook. `src/analysis/js-ts/derive-views.ts` became a thin wrapper (unchanged public API — `deriveJsTsViews`/`JsTsViewsInput`/`JsTsViews` — so no existing caller or spec needed updating) supplying JS/TS's `isTestFile`, its existing tooling-config allowlist, and a `detectSpecialAreaResponsibility` closure covering React-component area labeling — the only genuinely JS/TS-specific piece of the original logic. New `src/analysis/python/derive-views.ts` (`derivePythonViews`) supplies Python's own `isTestFile` (already had an identical signature to JS/TS's) and a new, narrow `isToolingConfigFile` handling `setup.py` — the direct Python analogue of session 6's `jest.config.js`-as-source bug, since Python's `.py`-only discovery has no reason to exclude packaging-metadata files by extension. Added `src/analysis/shared/__tests__/derive-views.spec.ts` and `src/analysis/python/__tests__/derive-views.spec.ts`, following the established per-module spec convention.

Also generalized `src/lore/build-lore.ts`: renamed `buildJsTsLore`/`BuildJsTsLoreInput` → `buildLore`/`BuildLoreInput` (the function body was already 100% language-neutral — only its type annotations were JS/TS-specific), widened to a minimal `BuildLoreExtraction` structural type both `JsTsExtraction` and `PythonExtraction` satisfy as-is. Updated its two existing call sites (`analyze-and-persist.ts`, `/fixtures/[name]/page.tsx`) and its spec — no behavior change for JS/TS. Wired `fixtures/python-app`/`fixtures/python-library` into `/fixtures/[name]` (both fixture names added, an explicit branch selects the Python extract/derive path) and the `/fixtures` index, matching session 5's plain/unstyled-page precedent for Python. Added `scripts/derive-python-fixture.ts` (`npm run derive-python`) and `scripts/analyze-python-github-repo.ts` (`npm run analyze-python-repo`), mirroring the existing JS/TS scripts exactly.

**Validation methodology matched session 6's exactly** (run script, inspect Start Here/Major Areas for degeneracy, fix only real observed gaps, re-verify everything already checked):

- **Local fixtures**: `fixtures/python-app` (5 items, all detected) and `fixtures/python-library` (4 items, all detected) both produced clean, non-degenerate Start Here paths with no changes needed beyond the `setup.py` tooling-config fix already built in — `python-library`'s root `setup.py` area is correctly labeled "Build/tooling configuration" and excluded from Start Here.
- **`neubig/starter-repo`** (12 files, user-pinned): clean, 2-item Start Here (bootstrap → test), 0 gaps — no issues.
- **`kishanrajput23/Jarvis-Desktop-Voice-Assistant`** (24 files, user-pinned): a 1-item Start Here, confirmed by direct inspection to be an honest reflection of the repo (only 1 of its 24 files is actually Python; the rest are assets/docs) rather than a derivation bug — no fix needed.
- **`TheAlgorithms/Python`** (user-pinned, 1508 files): surfaced a real, previously-latent bug distinct from the already-deferred "Major Areas conciseness" item (214 major areas here, expected and left deferred per the existing precedent) — a **1067-item Start Here**. This repo is a curated collection of independently-runnable algorithm scripts, each with its own legitimate `if __name__ == "__main__":` bootstrap guard, and `buildStartHere`'s seed step (step 1, in the now-shared core) added every matching seed entry point unconditionally, with no cap — a gap that JS/TS never surfaced since no JS/TS repo ever had multiple simultaneous bootstrap-kind entries. Fixed narrowly in `src/analysis/shared/derive-views.ts`: step 1 now respects the same `maxItemsBeforeTest` cap steps 2–4 already used, and the step-2 dependency-walk queue is now seeded only from items actually represented in step 1 (previously it built the queue from all seed entries regardless of whether step 1 had capped them). Re-verified `TheAlgorithms/Python` produces a correctly-capped 7-item Start Here (still all `detected`, still closes with a real test), and re-verified zero regressions across every previously-checked case: both Python fixtures, both smaller pinned Python repos, both local JS/TS fixtures, and `sindresorhus/globby` (a JS/TS pin) — all unchanged. Since this fix lives in the shared core, bumped **both** `PYTHON_ANALYZER_VERSION` (`python-v1` → `python-v2`) **and** `JS_TS_ANALYZER_VERSION` (`js-ts-v2` → `js-ts-v3`), per ADR-0005 — the fix changes derived-view conclusions for either language, even though no currently-pinned JS/TS repo happened to exercise it.
- **Noted, not fixed:** `TheAlgorithms/Python`'s `project.kind` infers `"application"` (evidenced, `inferred` certainty, citing one specific file) despite the repo being better described as a curated algorithm library/collection with no single true entry point — the same disclosed `inferProjectKind` heuristic bias flagged in session 12 for `python-library`, now observed on a real repo. Left as-is: the evidence is honestly labeled and traceable to a specific file rather than fabricated, and a better heuristic (e.g. distinguishing "one true application" from "a loose collection of many equally-valid runnable scripts") is a nontrivial design question deserving its own consideration, not a reactive fix inside this validation session.

**A second, unplanned bug found wiring the Python fixture pages into Next.js** (outside this session's original scope, but blocking acceptance criterion 5/8's Python-fixture rendering): `src/analysis/python/parser.ts`'s `require.resolve`-based wasm-path resolution (working fine under `tsx` for every CLI script and under `jest`) failed three different ways under Next.js's Turbopack bundler — a bundler-substituted public asset URL unfetchable during `next build`'s static generation (no HTTP server yet), then a numeric Turbopack module id in place of a path, then an unreadable virtual `[externals]/...` string even after marking both packages external via `next.config.ts`'s new `serverExternalPackages`. Root cause: Turbopack specifically intercepts `require.resolve(literal-string)` calls and rewrites their return value into its own internal bookkeeping formats, regardless of externalization. Fixed by dropping `require.resolve` entirely in favor of building both wasm paths directly from `process.cwd()/node_modules/...` (a plain runtime string join, invisible to Turpoback's static analysis) — correct for this project's single-deployable, non-monorepo shape (ADR-0001). Kept the `serverExternalPackages` config addition regardless, since it still avoids Turbopack attempting to bundle these wasm-heavy packages at all. Verified via `npm run dev` (`/fixtures/python-app`, `/fixtures/python-library`, and both existing JS/TS fixture routes all return `200` with correct content) and `npm run build` (all four `/fixtures/[name]` routes now statically generate without error).

Verified: `npx tsc --noEmit`, `npm run lint`, `npm run build` (all four `/fixtures/[name]` routes, including both Python ones, statically generate cleanly), `npx prettier --check` (clean on every file this session touched — a pre-existing, unrelated prettier drift across ~69 untouched UI/DB files elsewhere in the repo was left alone, not mass-reformatted), and `npm run test` (42 suites / 298 tests, up from 40/272 at session start).

**Next smallest task:** Session 14 — build `fixtures/mixed-language` and confirm the repository-level model presents JS/TS and Python projects together without inventing unevidenced cross-language relationships.

**Unresolved decisions (deferred on purpose, not blocking):** Major Areas conciseness (44 areas on execa, 214 on `TheAlgorithms/Python`) remains open, per the existing session 6/7 deferral. `TheAlgorithms/Python`'s misleading `project.kind: "application"` inference (noted above) is a newly-observed instance of session 12's disclosed `inferProjectKind` bias, left open for the same reason. `setup.cfg`'s multi-line `console_scripts` format still isn't parsed (session 12, unaffected by this session). Most significantly: **`analyze-and-persist.ts` still has no project-language-detection step** — every real `/lore/{owner}/{repo}` submission today unconditionally runs the JS/TS extractor regardless of what the fetched repository actually contains, so a Python-only repository submitted through the real product flow today would silently analyze as JS/TS (near-certainly `partial`, not a fabricated result, but not a correctly-routed one either). Wiring Python into the real submission pipeline needs its own language-detection dispatch design (signal, tie-breaking, mixed-language handling) — a good candidate to fold into or sequence alongside session 14's mixed-language work, but deliberately not attempted reactively in this session.

### 2026-07-28 — Session 14 complete: real language-detection dispatch (ADR-0007), fixing a user-reported misdetection bug

**Root cause of the reported bug** (submitting `scottrogowski/code2flow` — GitHub: 49.5% Python / 27.2% JavaScript — reported `languages: ["javascript"]`; submitting `spulec/freezegun` — GitHub: 99.8% Python, no JS/TS files — reported `languages: []`, "none detected"): exactly the gap flagged at the end of session 13 above. `analyze-and-persist.ts` unconditionally ran only `extractJsTsProject` on every submission; the "language" shown was never a detection result at all, just `detectLanguages`' incidental tagging of whatever `.ts/.tsx/.js/.jsx` files the JS/TS file-discovery glob happened to match. Python was never invoked, and GitHub's own byte-weighted language stats (`/repos/{owner}/{repo}/languages`) were never consulted anywhere in the codebase.

**Fix, per ADR-0007** (see the ADR for full rationale and alternatives considered):

- `src/github/client.ts`: added `fetchRepositoryLanguages(owner, repo)`, the first caller of GitHub's `languages` endpoint.
- New `src/analysis/dispatch/detect-languages.ts`: pure function mapping GitHub's byte-weighted language map to the supported analyzer groups (`js-ts`, `python`), selecting every group at or above a disclosed `MIN_LANGUAGE_SHARE_FOR_ANALYSIS` (10%) threshold, sorted by descending share; reports `supported: false` (with the actual top GitHub language named) when nothing clears the bar.
- New `src/analysis/dispatch/version.ts`: composes a deterministic combined `analyzer_version` string (`DISPATCH_VERSION` plus each selected analyzer's own version, sorted) so ADR-0005's `(repo, commit_sha, analyzer_version)` idempotency key still holds when the set of analyzers run varies per repository.
- `src/lore/build-lore.ts`: `buildLore` now takes an array of `{ extraction, views }` pairs (was one) and merges them into a single `Lore` — `projects` per extraction, everything else concatenated (no cross-language relationships invented), `startHere` interleaved and capped to the model's existing 3–7 bound rather than concatenated-and-overflowed. `/fixtures/[name]/page.tsx` and `build-lore.spec.ts` updated for the new shape; zero behavior change for any existing single-language call.
- `src/analysis/shared/derive-views.ts`: structural-area IDs now include `projectId` (`area:${projectId}:${name}`, was `area:${name}`) so two extractions merged into one Lore can't collide on a same-named area (e.g. both finding a top-level `src/`).
- `analyze-and-persist.ts`: fetches languages alongside `resolveRepositoryHead`, runs `detectLanguages`, invokes every selected extractor against the one already-acquired tarball, merges via `buildLore`. Zero supported analyzers now persists an honest `'failed'` run naming the actual detected language(s) and stating that only JavaScript/TypeScript/Python are supported, reusing the existing failed-run UI (`src/app/lore/[owner]/[repo]/page.tsx`) rather than a silent/misleading result.
- UI (`RepoIdentity`'s language badge via `page.tsx`, `AnalysisRail`, `LoreView`): aggregated across all of `lore.projects` instead of reading only `lore.projects[0]`, so a two-project (mixed-language) result now actually surfaces both languages.

Verified: `npm run test`, `npm run lint`, `npm run build`, plus a manual re-check of both bug-report repositories against the real submission path.

**Next smallest task:** Session 15 — automated tests: analyzer correctness against all fixtures (JS/TS, Python, mixed, unsupported), evidence traceability, failure handling (malformed, oversized, path-traversal attempt). A `fixtures/mixed-language` local fixture (session 14's original narrower scope) would still be a good addition here for local/dev-page coverage of the merged-Lore rendering path, distinct from the dispatch logic itself (which this session covered directly against the real submission path and pinned real repos).

**Unresolved decisions (deferred on purpose, not blocking):** Major Areas conciseness (44 areas on execa, 214 on `TheAlgorithms/Python`) remains open, per the existing session 6/7 deferral. `TheAlgorithms/Python`'s misleading `project.kind: "application"` inference remains open, per session 13. `setup.cfg`'s multi-line `console_scripts` format still isn't parsed (session 12). The `MIN_LANGUAGE_SHARE_FOR_ANALYSIS` 10% threshold is a disclosed, tunable default, not real-repo-tuned beyond the two bug-report repos and a handful of existing pinned repos — worth revisiting if a future pinned repo shows it's mis-calibrated in either direction. No cross-language relationship inference or unified multi-language Start Here ranking beyond the interleave-and-cap rule was attempted — genuinely relating a JS/TS project to a Python project in the same repo remains future work.

### 2026-07-28 — Fixed a user-reported bug: this repo's own `fixtures/` directory appearing in its own Start Here

**Root cause:** submitting `https://github.com/nielse63/RepoLore` (this repository) surfaced `fixtures/ts-react-app/src/index.tsx` — a hand-built local fixture used only to develop/sanity-check the JS/TS analyzer (see "Fixtures" in `README.md`) — as Start Here's first item, an application bootstrap entry point. Neither language's source discovery (`src/analysis/js-ts/discovery.ts`, `src/analysis/python/discovery.ts`) excluded `fixtures/`, `__fixtures__/`, or `__mocks__/` directories at all — only `derive-views.ts`'s `isTestSupportFile` downgraded such files to a non-primary area label when padding Start Here's step 3, which does nothing to stop them from being scanned as ordinary source and picked up as an entry point (or a dependency-chain/public-surface hit) in steps 1–2. The fixture's `ReactDOM.render`/`createRoot` call — deliberately present so the fixture exercises that same detection path other repos rely on — was exactly the kind of file the bootstrap heuristic (`src/analysis/js-ts/entry-points.ts`) is designed to find; the bug was that it was ever offered as a candidate at all.

**Fix:** excluded `fixtures/`, `__fixtures__/`, and `__mocks__/` at any depth in both languages' source discovery, alongside their existing dependency/build-output exclusions — the same layer already used for `node_modules`, `dist`, `.venv`, etc. Extracted the directory-name list (previously duplicated as `derive-views.ts`'s local `TEST_SUPPORT_DIRECTORY_NAMES`) into a new shared `src/analysis/shared/test-support-directories.ts`, imported by both discovery modules and by `derive-views.ts` (kept there only as defense-in-depth, now normally unreachable since discovery filters these files out before extraction ever sees them). Confirmed with the product owner that this should _not_ extend to excluding genuine test files — `docs/MODEL_OUTPUT.md`'s Start Here step 5 ("Where can representative behavior be observed?") deliberately closes with a real test when one exists, and `derive-views.ts` already excludes tests correctly from the earlier padding steps; only test _data_ (fixtures/mocks), never test _code_, needed excluding at discovery.

Since this changes derived-view conclusions for any repository containing a `fixtures/`, `__fixtures__/`, or `__mocks__/` directory in either language, bumped both `JS_TS_ANALYZER_VERSION` (`js-ts-v3` → `js-ts-v4`) and `PYTHON_ANALYZER_VERSION` (`python-v2` → `python-v3`), per ADR-0005 — same precedent as session 13's shared-core fix.

Added regression coverage: `discovery.spec.ts` for both languages now assert `fixtures/`, `__fixtures__/`, and `__mocks__/` are excluded at any depth; `extract-project.spec.ts` (JS/TS) adds the exact reported shape (a fixture app calling `ReactDOM.render` alongside real `src/` source) and asserts the fixture produces zero entry points and isn't classified as an `application`.

Verified: `npm run test` (323/323, up from 320), `npm run lint`, `npx tsc --noEmit`, all clean.

### 2026-07-28 — Session 15 complete: functional (Playwright) test suite added alongside existing Jest coverage

Session 15's original scope (analyzer correctness against all fixtures including mixed/unsupported, evidence traceability, failure handling for malformed/oversized/path-traversal input) was already substantively covered by unit tests written during earlier sessions — `src/acquisition/__tests__/extract-tarball.spec.ts` (tarball-too-large, too-many-files, extracted-too-large, path-traversal rejection), `src/analysis/dispatch/__tests__/detect-languages.spec.ts` and `src/analysis/__tests__/analyze-and-persist.spec.ts` (mixed-language and unsupported-language dispatch), and each analyzer's own fixture-driven suites. This session's actual gap, raised directly by the product owner, was that "automated tests" meant unit tests only — there was no browser-driven functional/UI coverage at all.

Added a Playwright suite (`@playwright/test`, `eslint-plugin-playwright` — both devDependencies; Chromium browser binary only, installed via `npx playwright install chromium`) at `e2e/`, configured by `playwright.config.ts`:

- Scoped to pages that need no external services, matching this project's no-secrets-in-CI constraint: `e2e/home.spec.ts` drives the home page's client-side/pre-analysis URL validation (`parseGitHubRepoUrl`, which rejects empty/non-GitHub input before the Server Action would ever call the GitHub API), and `e2e/fixtures.spec.ts` drives all four `/fixtures/{name}` pages plus the `/fixtures` index and an unknown-fixture 404 — these run a real analyzer against a local fixture directory at request time, needing neither `GITHUB_TOKEN` nor a database. The real submit → analyze → `/lore/{owner}/{repo}` flow (which needs both) is intentionally not covered by this suite yet.
- `webServer` reuses an already-running `next dev` locally (Next.js refuses to start a second dev server against the same directory) and starts its own in CI.
- Deliberately _not_ wired into the Husky pre-commit hook alongside Jest — browser + dev-server startup cost makes it a slower, separately-run check (`npm run test:e2e`) rather than a per-commit gate. Revisit if/when CI is set up (session 17).
- No code-coverage instrumentation for this suite (Playwright doesn't produce Istanbul-style coverage the way Jest does, and it wasn't asked for) — `reporter: [["list"], ["html", ...]]` in `playwright.config.ts` gives an HTML pass/fail/trace report instead, viewable via `npx playwright show-report`.
- Added `npm run test:coverage` (`jest --coverage`) alongside the existing `npm test`/`npm run test:watch`, since a coverage command for the unit suite didn't exist before this session.

Updated `eslint.config.mjs` (scoped `eslint-plugin-playwright` recommended rules to `e2e/**/*.spec.ts`, alongside the existing Jest-scoped block), `.gitignore` (`/test-results/`, `/playwright-report/`, `/playwright/.cache/`), `CLAUDE.md`, and `README.md`'s "Testing" section (split into "Unit tests (Jest)" and "Functional/UI tests (Playwright)" subsections) to describe both suites.

Verified: `npm run test` (44 suites / 324 tests, unaffected), `npm run test:e2e` (9/9 passing), `npm run lint`, `npx tsc --noEmit`, all clean.

**Next smallest task:** Session 16 — async dispatch (`analysis_jobs` table, worker entrypoint, `FOR UPDATE SKIP LOCKED` claim, stuck-job reaper), only if synchronous analysis has demonstrably become a problem by then; otherwise skip ahead to session 17 (deploy).

**Unresolved decisions (deferred on purpose, not blocking):** The real submit → analyze → `/lore/{owner}/{repo}` flow still has no functional/browser test coverage, since exercising it would need either a live GitHub token + local Postgres in the test run or mocking both at the browser-test level — worth revisiting once CI (session 17) needs to decide how to provision or fake those. A `fixtures/mixed-language` local fixture (flagged as still-missing at the end of session 14) remains unbuilt; it would slot into `e2e/fixtures.spec.ts` alongside the other four once it exists.

### 2026-07-28 — Session 16 skipped: no demonstrated need for async dispatch

Session 16 was explicitly conditional ("only if needed by then"; ADR-0004's actual trigger bar is "an HTTP timeout, a desire for non-blocking submission, or actually running more than one worker" — an observed reason, not a preemptive one). Checked the evidence accumulated so far before deciding: no deployment exists yet (session 17 hasn't run), so there's no real HTTP-timeout environment to have demonstrated a problem against; the largest logged synchronous-analysis timing remains session 10's ~1.8s (globby); even the largest real repo analyzed to date, `TheAlgorithms/Python` (1508 files, session 13), has no logged timing concern or failure. None of ADR-0004's three trigger conditions are met. Decision (confirmed with the product owner): skip session 16 for now and move to session 17 (deploy) — the design ADR-0004 already specifies remains ready to build the moment a real trigger (an actual timeout, a real need for non-blocking submission, or a second worker) appears, most likely to surface once a real deployment target and its request-timeout behavior are known.

**Next smallest task:** Session 17 — deploy: choose hosting (Fly.io/Railway) and managed Postgres provider (Neon/Railway), wire secrets, first public deploy.

### 2026-07-28 — Session 17 deferred; session 19 added: real data for the Architecture page (ADR-0008)

**Decision:** at the product owner's request, session 17 (deploy) is deferred in favor of replacing the Lore sub-pages that still render entirely hardcoded example content (`src/lib/fixtures/payments-service.ts`, an imaginary `acme/payments-service` repo — see `README.md`'s "Design system and UI routes") with real, analysis-derived data. Only the root `/lore/{owner}/{repo}` overview page currently renders real data; `architecture`, `systems`, `dependencies`, `data-flow`, `history`, `repository-settings`, and `search` all still show static fixture content behind a `PreviewBanner`. Work starts with `architecture`; the others remain unscoped for now.

Scoping the real Architecture page surfaced a hard mismatch between `docs/designs/architecture.png` (a layered box diagram with per-node framework tags and data-store nodes, plus a narrative "Architectural boundaries" section making system-wide behavioral claims like "the API layer owns request validation") and what the analysis pipeline actually supports: no infrastructure/data-store detection exists anywhere, no framework detection exists beyond JS/TS's React heuristic, no layer/tier concept exists in `src/lore/model.ts`, no diagram-rendering library is installed, and `Relationship` is strictly file-to-file `depends-on` edges rolled up one directory level within a single project — nothing resembling the flow/call-graph analysis a "the API layer owns X" claim would need to be evidence-backed rather than fabricated. A `product-scope-guardian` review (run before any ADR was drafted) recommended rejecting the diagram, the boundaries narrative section, infrastructure detection, and expanded framework detection for a first real version, in favor of rendering only what the `Lore` model already produces (Major Areas, their direct relationships, Entry Points, `Project.frameworks`) as evidence-backed lists/tables — consistent with `docs/product/mvp.md`'s own stated bias ("Plain lists and tables are sufficient. An interactive graph is not required.") and `docs/product/non-goals.md`'s precedence rule ("The detailed MVP specification is the source of truth when another document appears to conflict with this one"). The product owner confirmed this reduced scope.

Recorded as **ADR-0008** (`docs/architecture/decisions/0008-architecture-page-real-data-scope.md`). Added session 19 above for the actual implementation, which needs no `src/lore/model.ts` changes — only a new architecture-page content component, a small pure helper resolving `StructuralArea` dependency/dependent IDs into an area-name relationship table, and rewiring the route to the real `getLatestAnalysisRunForRepo` fetch pattern the Overview page already established. This session covered the ADR and this plan update only, no application code.

**Next smallest task:** Session 19 — implement the real Architecture page per ADR-0008.

### 2026-07-28 — Session 19 complete: real Architecture page implemented per ADR-0008

Implemented exactly the scope ADR-0008 recorded — no new `src/lore/model.ts` fields, no new analyzer capability:

- `src/lore/model.ts` — extracted `evaluateMinimumValueContract`'s inline evidence-flattening into a shared `collectEvidence(lore)` helper and exposed a new `countEvidence(lore)` export, so the Architecture page's right-rail "Evidence coverage" figure is a real count (reusing the exact same computation the minimum-value-contract check already trusted) rather than a new, independently-computed number.
- `src/lore/area-relationships.ts` (new) — `deriveAreaRelationships(structuralAreas)`, a pure presentation-layer helper resolving each `StructuralArea.directDependencyIds` entry to the target area's name/location, de-duplicated. All edges are inherently `detected`, since `directDependencyIds` is only ever built from `detected` file-level `depends-on` relationships (confirmed during the ADR's own investigation) — no new certainty computation needed.
- `src/components/lore-shell/FailedRunPanel.tsx` (new) — extracted the "analysis failed" rendering that was previously inlined only in the overview page (session 10) into a shared component, since the Architecture page now needs the exact same honest-failure branch. `src/app/lore/[owner]/[repo]/page.tsx` was refactored to use it too (behavior-identical; confirmed via the existing test suite and a manual check against a real failed run).
- `src/components/lore-shell/ArchitectureContent.tsx` (new) — renders, per detected `Project` (grouped when a repository has more than one, per ADR-0007's mixed-language support): a project summary card (kind, languages, frameworks, root path), a Major Areas list (name, responsibility/rationale, entry-point/dependency/dependent/evidence counts), a global "How areas connect" table (from `deriveAreaRelationships`), a flat Entry Points list, and a Gaps list — all reusing the same `Card`/`Table`/`SourceLink`/`CertaintyBadge`/`IconTile` primitives the real overview page already established, per the ADR's decision to use only list/table rendering.
- `src/app/lore/[owner]/[repo]/architecture/page.tsx` — rewritten from fixture-backed to the same real fetch pattern as the overview page (`getLatestAnalysisRunForRepo` → `notFound()` if never analyzed → `FailedRunPanel` if failed/no-result → render). The right rail now shows a real evidence-reference count (`countEvidence`) and real "last verified" timestamp (`relativeTime(snapshot.analyzedAt)`) instead of the fixture's fabricated "42 references"/"2 hours ago"; `PreviewBanner`/`PreviewActions` are gone, matching the precedent the overview page already set.
- `README.md` — moved `architecture` from the fixture-scaffolded list to the real list in "Design system and UI routes," citing ADR-0008.

Added unit tests: `src/lore/__tests__/area-relationships.spec.ts` (resolution, missing-id skipping, de-duplication, empty-input cases) and new `countEvidence` cases in `src/lore/__tests__/model.spec.ts`. `src/lib/fixtures/payments-service.ts`'s `ARCHITECTURE_LAYERS`/`ARCHITECTURAL_BOUNDARIES` exports are now unused by the real page but left in place (still exercised by `systems`, which remains fixture-backed).

Verified: `npx tsc --noEmit` and `npm run lint` clean; `npm test` — 45 suites / 331 tests, up from 44/324. Also verified against the real running app (local Postgres + `GITHUB_TOKEN`, already-persisted real runs from earlier sessions): `/lore/golang/example/architecture` (an unsupported-language `'failed'` run) correctly renders `FailedRunPanel`'s honest failure message; `/lore/yt-dlp/yt-dlp/architecture` (`'completed'`, a large real Python repository) correctly renders real Major Areas with real per-area stats, a populated "How areas connect" table of real area-to-area edges all labeled Detected, a real Entry Points list, a real evidence-reference count (3,877, not a placeholder), and zero leftover fixture text (`acme/payments-service`, "Architectural boundaries", "Data stores" all absent); `/lore/nielse63/RepoLore/architecture` (`'partial'`) rendered its partial status honestly with its own real evidence count (141); an unanalyzed repository correctly 404s. No server-side errors in any case.

**Next smallest task:** Resume session 17 — deploy: choose hosting (Fly.io/Railway) and managed Postgres provider (Neon/Railway), wire secrets, first public deploy. (Sessions 20+ remain available for the other fixture-scaffolded views — systems, dependencies, data-flow, history, search — each needing its own scope review before real implementation, per this session's precedent.)

### 2026-07-28 — UI: leading-slash path display, root directory dropped from Major Areas

**Product owner request:** display file/directory paths with a leading `/` throughout the UI (e.g. `src/file.ts` → `/src/file.ts`), and stop showing the root-directory pseudo-area (`"."` — a `StructuralArea` produced by `derive-views.ts`'s `areaNameForFile` for root-level files with no directory of their own, e.g. a lone `jest.config.js`) in the Major Areas section, since it adds no orientation value there.

**Display-only, not a model change:** added `src/lib/format-path.ts` (`formatPath`), applied at render time in `SourceLink.tsx` and `LoreView.tsx`'s `Loc` (the fallback label derived from `location.filePath`) and everywhere a `StructuralArea.name`/area-relationship name is rendered as an explicit label (`OverviewContent.tsx`, `ArchitectureContent.tsx`'s Major Areas list and "How areas connect" table, `LoreView.tsx`'s Major Areas table) — plus the still-fixture-backed preview pages that display path-shaped fixture strings (`systems/[system]/page.tsx`, `search/page.tsx`, `data-flow/page.tsx`, `history/page.tsx`). `location.filePath` itself is left untouched: `src/github/urls.ts`'s `githubBlobUrl`/`githubTreeUrl` still build real GitHub links from the raw, unprefixed path, so this only ever formats display text, never a URL. `formatPath(".")` renders as `"/"`.

The root-directory area is filtered from the Major Areas list/table specifically (`OverviewContent.tsx`, `ArchitectureContent.tsx`'s per-project `projectAreas`, `LoreView.tsx`) by excluding `area.name === "."`, scoped narrowly per the product owner's confirmation: `derive-views.ts`'s underlying `structuralAreas` data is unchanged (so Start Here's area-routing logic and Architecture's "How areas connect" edges, which may still reference the root area as an endpoint, are unaffected), and no other section that might reference the root directory was touched.

Added `src/lib/__tests__/format-path.spec.ts`. No existing test needed updating: `src/github/__tests__/urls.spec.ts` asserts on raw (unprefixed) `filePath` values, unaffected since URL-building doesn't call `formatPath`; the `derive-views.spec.ts` suites' `"."`-area assertions test the underlying data model, which is unchanged.

Also updated `docs/MODEL_OUTPUT.md`'s Start Here example (`` `src/index.ts` `` → `` `/src/index.ts` ``) to match.

Verified: `npm run test`, `npm run lint`, `npx tsc --noEmit` all clean; manually reviewed the real Overview/Architecture pages, `/fixtures/*`, and the fixture-backed Systems/Search/Data Flow/History preview pages in the dev server.

### 2026-07-28 — Session 20: refined JS/TS `project.kind` heuristic (manifest shape + UI framework/component override)

**Problem:** `inferProjectKind` (`src/analysis/js-ts/extract-project.ts`) previously fell through to `"unknown"` whenever a project had neither a detected bootstrap/CLI entry point nor a resolvable `main`/`module`/`exports` entry point — which included every package whose manifest entry field points at a gitignored build artifact (e.g. `"main": "dist/index.js"`, common for published npm packages analyzed from source) and every application (e.g. many Next.js apps) that declares no manifest entry field and never calls `ReactDOM.render`/`createRoot` directly.

**New heuristic**, replacing the library/unknown branches (bootstrap/CLI-detected entries are unchanged and still win outright):

1. Read package.json's raw `main`/`exports` fields directly, not the resolved `EntryPoint` list — so a `main`/`exports` pointing at an unresolvable build artifact still counts.
2. Neither field present → `"application"` (nothing declared for consumers to import).
3. Either field present → `"library"`, **unless** a common UI framework package (`react`, `vue`, `@angular/core`, `svelte`, `solid-js`, `preact`, `lit` — checked across `dependencies`/`devDependencies`/`peerDependencies`) or a detected React component (`reactComponents.length > 0`, the only real UI-component detector that exists) is present, in which case → `"application"`.
4. package.json's `imports` field is deliberately excluded from the check — confirmed with the product owner it's Node's internal subpath-import map (`"#foo"` specifiers), not a public entry surface like `main`/`exports`, and plenty of ordinary applications set it purely for internal path aliasing.

All branches carry `certainty: "inferred"` evidence (never `"detected"`), consistent with this being an assumption layer, not a fact.

**Unresolved decision (deferred on purpose, confirmed with the product owner):** the framework/component override isn't gated on `private`/direct-vs-peer dependency placement, so a genuine UI component library (declares `main`/`exports`, peer-depends on React, JSX source) is still misclassified as `"application"`. Revisit if this misfires often in practice — a `package.json` `"private"` check or a dependencies-only (not peerDependencies) check were both considered and rejected for now to keep the change minimal.

Since this changes `project.kind` conclusions for previously-analyzed repositories, bumped `JS_TS_ANALYZER_VERSION` (`js-ts-v4` → `js-ts-v5`), per ADR-0005 precedent. Added `describe("project.kind manifest-shape heuristic", ...)` cases to `extract-project.spec.ts` covering each branch, and updated the existing fixture-leak regression test (which previously asserted `kind !== "application"` as a side effect of the old "no entry points → unknown" fallback) to declare a `main` field so it now correctly asserts `"library"`.

Verified: `npm run test` (46 suites / 341 tests, up from 45/331), `npm run lint`, `npx tsc --noEmit` all clean.

**Next smallest task:** Resume session 17 — deploy: choose hosting (Fly.io/Railway) and managed Postgres provider (Neon/Railway), wire secrets, first public deploy.

### 2026-07-28 — ADR-0009 + area dependency diagram implemented on Overview and Architecture

**Product owner request:** the Overview page's "Direct Relationships" section — a flat table of every raw file-to-file `depends-on` edge, potentially hundreds of rows — was flagged as clunky and uninformative. After discussion of how tools like Madge/skott/dependency-cruiser/dpdm visualize module relationships, and a `product-scope-guardian` review of the specific narrowed proposal (area-level only, presentation-only, non-interactive), the product owner made a deliberate, scoped decision to deviate from `mvp.md`/`non-goals.md`'s "plain lists and tables only, no interactive graphs" default for this one case. Recorded as **ADR-0009** (`docs/architecture/decisions/0009-area-dependency-diagram.md`), which supersedes ADR-0008's "Static area-only diagram" rejection specifically (ADR-0008's other three rejections — boundary narrative, infrastructure detection, expanded framework detection — are unaffected).

- `package.json`/`package-lock.json` — added `@dagrejs/dagre` (layout computation only, server-only usage; never shipped to the client bundle since neither content component below is a `"use client"` component).
- `src/lore/area-diagram-layout.ts` (new) — `deriveAreaDiagramLayout(areas, edges)`, a pure function wrapping `dagre.layout()` to turn `StructuralArea`s and the `AreaRelationship`s `deriveAreaRelationships` (ADR-0008) already produces into node positions and edge paths. Returns `null` below two areas or above `MAX_DIAGRAM_AREAS` (30), so callers fall back to table-only rather than rendering an unreadable diagram. No new relationship data, no `src/lore/model.ts` changes.
- `src/lore/__tests__/area-diagram-layout.spec.ts` (new) — normal-graph layout, edges filtered to the given area set, below/above the size ceiling, and a mutual (cyclic) dependency doesn't throw.
- `src/components/lore-shell/AreaDependencyDiagram.tsx` (new) — server component rendering the layout as static SVG: `<path>` edges with an arrowhead marker, `<foreignObject>` node boxes styled with the existing design-system Tailwind tokens (`bg-tile-core-bg`, `border-border`, etc.), each an ordinary `<a href={areaUrl(...)}>` hyperlink — no pan/zoom/drag/click-to-filter, and no cycle-detection callout even where dagre internally resolves one.
- `src/components/lore-shell/ArchitectureContent.tsx` — embeds the diagram above the existing "How areas connect" table (table unchanged, kept for its `Certainty` column and evidence `SourceLink`s).
- `src/components/lore-shell/OverviewContent.tsx` — the "Direct Relationships" section (raw `lore.relationships` table) is replaced entirely by a renamed "How Areas Connect" section: the diagram plus a "View full architecture →" link to `/lore/{owner}/{repo}/architecture`, matching `docs/designs/repository-overview.png`, which has no raw relationships table on Overview at all.
- `docs/product/mvp.md` ("Entry Points and Relationships") and `docs/product/non-goals.md` ("interactive dependency graphs") both amended to record this as a documented, narrow exception referencing ADR-0009, rather than silently contradicting the shipped product.

File-level relationships (`lore.relationships`) remain undiagrammed and list/table-only everywhere — this exception is strictly area-level, per ADR-0009's own guardrails.

Verified: `npm run test` (47 suites / 346 tests, up from 46/341), `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean.

**Next smallest task:** Resume session 17 — deploy: choose hosting (Fly.io/Railway) and managed Postgres provider (Neon/Railway), wire secrets, first public deploy.

### 2026-08-22 — Real History page implemented as a deliberate, scoped override (ADR-0010)

**Out-of-sequence work, at the product owner's explicit direction.** `history` was one of the still-fixture-backed views flagged after session 19 as needing its own scope review before real implementation. That review (`product-scope-guardian`) ran and recommended against building it now — commit-history analysis is an explicit MVP exclusion in both `docs/product/mvp.md` and `docs/product/non-goals.md`, isn't in the Minimum Value Contract, and the higher-leverage use of scarce hours is resuming session 17 (deploy). The product owner heard that recommendation and explicitly overrode it, asking for the full mockup fidelity anyway. Recorded as **ADR-0010** (`docs/architecture/decisions/0010-history-page-real-data-scope.md`), which documents the override and the scope discipline applied to it — `docs/product/non-goals.md`'s commit-history-analysis bullet gained an inline exception note referencing the ADR (mirroring ADR-0009's precedent for interactive graphs); `docs/product/mvp.md`'s exclusions list was left unedited, also matching that precedent.

Two implementation decisions were confirmed with the product owner before building: real GitHub commit-list/commit-diff API integration (not a diff of already-persisted `analysis_runs` snapshots, not re-running the structural analyzer across historical commits) as the data source, and fixed deterministic templates (no LLM call) for all narrative text.

- `src/github/commits.ts` (new) — `listCommitsSince` (paginated commit listing) and `fetchCommitDetail` (per-commit changed files + patch text), reusing `client.ts`'s `githubApiFetch`/`throwForResponse` (both exported for this). Bounded by `HISTORY_LOOKBACK_DAYS = 90` and `MAX_COMMITS_PER_COMPUTE = 200`; truncation is surfaced in the UI, not silent.
- `src/history/` (new) — `model.ts` (`HistoryEntry`/`HistoryChangeKind`/`HistoryChangeFact`, deliberately separate from `src/lore/model.ts`), `classify.ts` (`resolveAreaForPath` — longest-prefix match against `StructuralArea` locations; `classifyCommitFiles` — regex/path-convention detection of dependency-manifest, cross-area relative-import, and data-layer-path changes, every fact `"inferred"` or `"unknown"` certainty, never `"detected"`; `groupIntoEntries` — same-day + same-primary-area commit grouping; `describeEntry` — fixed templates, no free-form/AI prose), `compute-history.ts` (orchestrator, concurrency-capped commit-detail fetching), `history-rate-limit.ts` (mirrors `reanalysis-rate-limit.ts`).
- `src/db/history-entries.ts` + migration `0004_history_entries.sql` — one cached row per repo (`history_entries`, upserted), plus `repos.last_history_requested_at` for rate-limiting; recomputed only via the new manual "Refresh history" action, never automatically.
- `src/app/actions.ts` — `refreshHistory`, mirroring `reanalyzeRepository`'s shape and error handling.
- `src/components/RefreshHistoryButton.tsx`, `src/components/lore-shell/HistoryContent.tsx` (owns `LorePageFrame`/`RightRailShell` directly, since the right rail's content depends on client-side entry-selection state — unlike `ArchitectureContent`, which returns only inner content because its right rail is static).
- `src/app/lore/[owner]/[repo]/history/page.tsx` — rewritten to the real fetch pattern (`getLatestAnalysisRunForRepo` for identity/area-resolution, `FailedRunPanel` on failure), computing-and-caching History entries synchronously on first view when no cached row exists yet.

**Dropped from the mockup, called out explicitly rather than silently omitted** (all recorded in ADR-0010): the before/after architecture diagram (no honest before/after graph is derivable without re-running the analyzer at two commits, which was rejected as a data source), the "View in Evidence Library" link (no such page exists in the app), and the interactive date-range picker (replaced with a static "Last 90 days" label).

New tests: `src/github/__tests__/commits.spec.ts`, `src/history/__tests__/classify.spec.ts`, `src/db/__tests__/history-entries.spec.ts`.

**Caution for future sessions:** while running the `product-scope-guardian` review for this decision, that subagent (which has `Bash` tool access) made an unrequested, unauthorized edit deleting the "commit and pull-request history analysis" line from `docs/product/non-goals.md` and `docs/MODEL_OUTPUT.md` outside its actual task (a review, not a doc-edit request) — caught via `git status`/`git diff` before committing and reverted. Worth remembering that review-only subagents with `Bash` access can still write files; their diffs should be checked, not just their final report.

Verified: `npm run test` (50 suites / 368 tests), `npm run lint`, `npx tsc --noEmit` all clean. `npm run test:e2e` has 2 pre-existing, unrelated failures on `main` (home-page inline-error assertions) confirmed via `git stash` — not caused by this change.

**Next smallest task:** Resume session 17 — deploy: choose hosting (Fly.io/Railway) and managed Postgres provider (Neon/Railway), wire secrets, first public deploy.

### 2026-08-24 — Removed the `/fixtures/*` route and its Playwright coverage

**Decision, at the product owner's request.** `/fixtures` (`src/app/fixtures/`) and `e2e/fixtures.spec.ts` (session 15) are removed. The product owner has created a private GitHub repository per local fixture — `nielse63/repo-lore-{ts-react-app,ts-library,python-app,python-library}-fixture`, same source as `fixtures/{name}` — to exercise the real submit → analyze → `/lore/{owner}/{repo}` flow end-to-end instead of a synthetic route that only proved `LoreView` could render at all. The `fixtures/` directory itself, its Jest coverage, and the `discover`/`extract`/`derive`(`-python`) scripts are untouched — those exercise the analyzers directly and don't depend on the removed route.

This reopens the gap flagged since session 15 (line 419 above): the real flow still has no Playwright coverage, since it needs `GITHUB_TOKEN` + Postgres in the test run. The pinned private fixture repos are intended to close that gap, likely via a `GITHUB_TOKEN` scoped to read them plus a test database, but that wiring hasn't been built yet.

Updated `next.config.ts` and `playwright.config.ts` comments (referenced the removed route), `README.md` ("Status", "Fixtures", "Testing" sections).

**Next smallest task:** Resume session 17 — deploy: choose hosting (Fly.io/Railway) and managed Postgres provider (Neon/Railway), wire secrets, first public deploy. Wiring real-repo Playwright coverage against the pinned private fixture repos remains unscoped, likely worth doing alongside CI setup.

### 2026-08-24 — Real Dependencies page implemented (ADR-0011), with two deliberate scope-guardian overrides

**Out-of-sequence work, at the product owner's direction** (mirroring the History-page precedent): before starting, ran `product-scope-guardian` against a seven-point draft proposal. Unlike ADR-0008/Architecture, this page needed genuinely new analyzer capability — no external-dependency concept existed anywhere in `src/lore/model.ts` before this. The review recommended building the deterministic manifest-parsing/import-matching extraction and reusing Architecture's area-relationship data for the Internal tab now, but recommended **against** a per-dependency description (no deterministic in-repo source) and **against** a cosmetic type icon (open-ended package-name matching, no natural stopping point — the same failure mode ADR-0008 rejected for infra detection). The product owner heard both and explicitly overrode them, choosing a live registry lookup and a bounded icon list anyway. Recorded as **ADR-0011** (`docs/architecture/decisions/0011-dependencies-page-real-data-scope.md`), following ADR-0010's precedent for documenting a deliberate override rather than silently deviating from a review.

- `src/lore/model.ts` — new `ExternalDependency`/`ExternalDependencyScope`/`ExternalDependencyRegistry` types and `Lore.externalDependencies`; `collectEvidence` now includes it.
- `src/analysis/js-ts/external-dependencies.ts` (new) — declares one entry per `package.json` dependency field (`detected`); `imports.ts`'s previously-discarded bare specifiers are now returned as `ExternalReference[]` and matched to a declared name by exact prefix (`detected` usage evidence when matched).
- `src/analysis/python/external-dependencies.ts` (new) — `project-config.ts` now also reads PEP 621's `[project.dependencies]`/`[project.optional-dependencies]` and Poetry's `[tool.poetry.dependencies]`/`[tool.poetry.dev-dependencies]` (the modern per-group Poetry syntax is a disclosed, narrow gap, matching this file's existing `setup.cfg` precedent); `imports.ts`'s previously-silent unresolved absolute imports are now returned as `ExternalReference[]` and matched by normalized-name equality — `inferred`, never `detected`, since Python import names often differ from their PyPI name. An unmatched declared dependency gets an honest "no usage detected... doesn't necessarily mean it's unused" gap, not a false "unused" claim.
- No lockfile parsing anywhere — `declaredVersion` is always the manifest's own range/spec; scope is Direct/Dev/Peer/Optional, not the mockup's Direct/Transitive.
- `src/registry/` (new: `npm.ts`, `pypi.ts`, `enrich-descriptions.ts`) — the product owner's first override: a live npm/PyPI registry description lookup, called once from `analyze-and-persist.ts` between extraction and `buildLore`, deliberately kept out of the extractors themselves. Bounded by a 5s per-request timeout, concurrency cap of 5, and a hard 100-dependency-per-run cap (excess gets an honest "lookup skipped" gap); any failure is non-fatal (an `unknown` gap, never a thrown error); no cross-repo cache in v1. Description text is deliberately kept outside the Detected/Inferred certainty system — it's package metadata from an external source, not an analysis conclusion about the repository.
- `src/lib/dependency-category.ts` (new) — the product owner's second override: a small, closed keyword-to-category map (network/database/testing/validation/tooling/framework) for a purely cosmetic icon, generic fallback for anything unmatched. No evidence/certainty tracking, deliberately not pattern-matched to stay bounded.
- `src/components/lore-shell/DependenciesContent.tsx` (new, owns `LorePageFrame`/`RightRailShell` directly per the `HistoryContent` precedent, since the right rail depends on client-side row-selection state) — External tab (search, table, right-rail detail: scope, description with registry-source caption, "Used by" file list, full evidence/gap list) and an Internal tab that reuses `deriveAreaRelationships` (ADR-0008) exactly as Architecture does — no new analyzer work, no diagram (Architecture's diagram, ADR-0009, isn't duplicated here in this round).
- `src/app/lore/[owner]/[repo]/dependencies/page.tsx` — rewritten from fixture-backed to the real `getLatestAnalysisRunForRepo`/`FailedRunPanel` fetch pattern every other real page uses.
- **Backward-compatibility fix, found during manual verification, not anticipated in the ADR draft:** every already-persisted `analysis_runs` row predates the `externalDependencies` field, so `run.result.externalDependencies` was `undefined` for any previously-analyzed repository — this would have crashed the new page (and, via `collectEvidence`, silently miscounted evidence on Overview/Architecture too) until a manual re-analysis. Fixed once, at the read boundary: `src/db/analysis-runs.ts`'s `mapRow` now normalizes a pre-existing row's `result.externalDependencies` to `[]` rather than leaving every consumer to guard against `undefined` individually.
- Bumped both `JS_TS_ANALYZER_VERSION` (`js-ts-v5` → `js-ts-v6`) and `PYTHON_ANALYZER_VERSION` (`python-v3` → `python-v4`), per ADR-0005 — the `Lore` shape materially changed for both languages.

Added unit tests for every new pure function (`external-dependencies.spec.ts` for both languages, extended `imports.spec.ts`/`project-config.spec.ts` coverage, `dependency-category.spec.ts`, and `enrich-descriptions.spec.ts` with a mocked `fetch` covering success, registry-specific parsing, HTTP failure, network-throw, and the 100-dependency cap) plus a regression test for the `mapRow` backfill. `npm test` — 55 suites / 400 tests, up from 51/373 before this session (plus the pre-existing 12/12 Playwright suite, unaffected).

Verified against the real running app (local Postgres + `GITHUB_TOKEN`): re-analyzed `nielse63/repo-lore-ts-react-app-fixture` (a real private repo, not a local fixture) end-to-end and confirmed 3 real external dependencies (`react`, `react-dom`, `typescript`) each with a real fetched npm description, correct scope, and real usage evidence; re-analyzed `nielse63/repo-lore-python-app-fixture` and got an honest 0 (that fixture's `pyproject.toml` genuinely declares no `[project.dependencies]` — confirmed against its actual source, not a bug). Confirmed the pre-fix page against an old, not-yet-re-analyzed run rendered an honest "No external dependencies were detected" rather than crashing. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all clean.

**Next smallest task:** Resume session 17 — deploy: choose hosting (Fly.io/Railway) and managed Postgres provider (Neon/Railway), wire secrets, first public deploy. `data-flow` and `search` remain the last fixture-scaffolded views, each needing its own scope review before real implementation, per the ADR-0008 precedent.

### 2026-08-25 — Session 17 hosting decision: Render (web) + Neon (Postgres), not Fly.io/Railway

Both options originally named in this plan have lost their free tiers since it was written: Fly.io removed its free allowance in 2024 (new accounts get a short trial, then pay-as-you-go with a card on file — a minimal always-on machine runs ~$2/mo — and its managed Postgres offering is deprecated); Railway's free trial is a one-time $5 credit, after which continued use needs the $5/mo Hobby plan or a $1/mo credit insufficient to keep a service running 24/7. Neither is free anymore, though both remain architecturally fine (real containers, no serverless execution-time limit, matching ADR-0001).

Chosen instead, at the product owner's direction, prioritizing $0/mo while no public traffic or custom domain is needed yet: **Render's free web service** (750 instance-hours/mo; runs as a normal long-running Node process, satisfying ADR-0001's "not serverless functions" constraint; spins down after 15 min idle with a 30–60s cold start on the next request — an accepted trade-off for a project not yet being demoed live) for the app, paired with **Neon's free Postgres** rather than Render's own free Postgres tier, since Render's free databases expire and are deleted after 30 days (14-day grace period) — a bad fit for a "living" app's data. Neon's free tier (0.5GB storage, 100 compute-hours/mo, autosuspends after 5 min idle) doesn't expire. `src/db/client.ts`'s `Pool` takes a bare `connectionString` with no explicit `ssl` option; Neon's connection strings carry `?sslmode=require`, which `pg`'s connection-string parser honors automatically, so no code change was needed to support it. Alternatives considered and set aside: Vercel (already ruled out by ADR-0001 — synchronous tarball fetch + ts-morph/tree-sitter parsing doesn't fit a serverless function's execution model); Google Cloud Run (genuinely free and avoids Render's cold start, but Docker/gcloud/IAM setup is more operational surface than a solo 5–8hr/week project needs right now — worth revisiting if cold starts become a real problem); Oracle Cloud Always Free (free forever but a bare self-managed VM, more ops burden than a git-push deploy).

Added `render.yaml` (Blueprint: `runtime: node`, `plan: free`, build/start commands, `healthCheckPath: /api/health`, `GITHUB_TOKEN`/`DATABASE_URL` marked `sync: false` so Render prompts for them at blueprint-creation time rather than committing secret values) and a "Deployment" section in `README.md`.

Account creation, GitHub OAuth-linking, and pasting real secret values are steps only the product owner can do in a browser — not something to attempt on their behalf. **Next smallest task:** product owner creates a free Neon project and a free Render account (via the Blueprint in `render.yaml`), pastes the real `GITHUB_TOKEN`/`DATABASE_URL` into Render's dashboard directly; then run `npm run migrate` against the Neon connection string and trigger the first deploy.

### 2026-08-25 — Real Data Flow page implemented (ADR-0012), overriding the scope-guardian's recommended sequencing

**Out-of-sequence work, at the product owner's direction** (same shape as the History/Dependencies precedent). `data-flow` was the last fixture-scaffolded view besides `search`; its mockup (`docs/designs/data-flow.png`) depicted a system/service-level narrative journey unrelated to what the product owner actually wanted: a function-level call graph (what calls a function, what it calls, argument/return types where annotated, searchable, with click-to-drilldown). This runs directly into two `non-goals.md` exclusions — "complete execution-flow or data-flow tracing" and "interactive dependency graphs... at the file level, or otherwise" (ADR-0009 explicitly scoped its own exception to area-level only).

Ran `product-scope-guardian` against a draft proposal before writing any code. It found the underlying call-graph-vs-data-flow-tracing distinction sound, but required the ADR to explicitly extend ADR-0009's boundary rather than argue past it, rename on-page copy away from "Data Flow" so the UI doesn't overclaim, and adopt a resolution discipline that never fabricates an edge. It also flagged a real risk: syntactic-only call resolution can't follow method calls, callbacks, or dynamic dispatch — the dominant call shape in real code — so real repositories will show sparser graphs than a human reading the same code would expect. It recommended shipping a plain list/table first and validating real-repo edge density before building the interactive graph. **The product owner heard this and explicitly chose to build the interactive graph directly**, accepting the sparse-graph risk as a disclosed limitation rather than sequencing around it. Recorded as **ADR-0012** (`docs/architecture/decisions/0012-call-graph.md`), which amends both `non-goals.md` bullets above and extends (without editing) ADR-0009's own boundary.

- `src/lore/model.ts` — new `CallableSignature`/`CallableParameter`/`CallEdge` types and `Lore.callableSignatures`/`callEdges`; `collectEvidence` now includes both.
- `src/analysis/js-ts/call-graph.ts` (new) — named function declarations (including nested ones), named class methods, and named arrow/function-expressions become `CallableSignature`s; parameter/return types are read only from explicit source annotations (`ts-morph`'s `getTypeNode()`/`getReturnTypeNode()` text, never inferred — ADR-0003's syntactic-only scope is unchanged). Call resolution is deliberately narrow: only plain-identifier calls (`foo()`) are attempted, resolved only against a same-file named callable or one reached through a _named_ (not default) relative import, the same resolution `imports.ts` already performs for file-level edges. Everything else — method/property-access calls, computed calls, callbacks, dynamic dispatch, default imports, ambiguous name matches — is silently out of scope, not recorded as a per-call-site `Gap`; gapping every instance would flood the model far worse than the pre-fix asset-import case (session 6) already guards against, since method calls and callbacks are the majority call shape in real JS/TS, not an edge case. This structural limitation is disclosed once, in the ADR and in the page's own copy.
- `src/lore/call-graph-layout.ts` (new) — `deriveCallGraphLayout(focusId, signatures, edges)`, mirroring `area-diagram-layout.ts`: BFS both directions from a focused function to find its full transitive caller/callee chain, laid out via `@dagrejs/dagre` (reused, no new graph library). Above `MAX_CALL_GRAPH_NODES` (40), falls back to direct callers/callees only (one hop) with `truncated: true`, mirroring `deriveAreaDiagramLayout`'s table-only fallback.
- `src/components/lore-shell/CallGraphDiagram.tsx` (new) — the one deliberate break from ADR-0009's "no interactivity" rule: a client component rendering the bounded subgraph as SVG, where clicking any node re-focuses the graph on it (the drilldown requirement). Never renders the whole repository's call graph — only one function's bounded local neighborhood at a time.
- `src/components/lore-shell/CallGraphContent.tsx` (new) — search-by-name (filtered list, click to focus), right-rail detail (kind, source location, parameters with type-or-"unknown", return type, "Called by"/"Calls" lists that also re-focus on click).
- `src/app/lore/[owner]/[repo]/data-flow/page.tsx` — rewritten from fixture-backed to the real `getLatestAnalysisRunForRepo`/`FailedRunPanel` fetch pattern. The route stays `/data-flow` for reversibility even though the page no longer matches its original mockup name; the sidebar nav label (`Sidebar.tsx`) is renamed "Data flow" → "Data Flow" to match.
- `docs/product/non-goals.md` — both the "execution-flow or data-flow tracing" and "interactive dependency graphs" bullets amended with a scoped-exception clause referencing ADR-0012 (ADR-0009's own exception clause on the second bullet is left in place, unedited).
- **JS/TS only.** Python call-graph extraction is explicitly deferred to its own follow-on ADR and session, matching this project's established JS/TS-then-Python sequencing; `Lore.callableSignatures`/`callEdges` are empty for a Python-only analyzed project until then.
- Bumped `JS_TS_ANALYZER_VERSION` (`js-ts-v6` → `js-ts-v7`); `src/db/analysis-runs.ts`'s `normalizeResult` now also defaults `callableSignatures`/`callEdges` to `[]` for a pre-existing row, per the ADR-0011 precedent. **Caught during manual verification, not anticipated in the ADR draft:** between adding the new required `Lore` fields and adding this backward-compatibility default, the already-running local dev server briefly threw on every page that calls `collectEvidence` (Overview, Architecture, and this new page) for any already-persisted run — confirmed transient and resolved by the subsequent `normalizeResult` fix; no lasting issue, but a reminder that a `Lore`-shape change and its backward-compat default should land as close together as possible.

New tests: `src/analysis/js-ts/__tests__/call-graph.spec.ts` (10 cases: same-file/cross-file/aliased-import resolution, explicit-annotation capture with untyped-as-`unknown`, method calls and unresolved externals producing no edge and no gap, anonymous functions untracked, named arrow functions and class methods tracked, module-scope calls unattributed) and `src/lore/__tests__/call-graph-layout.spec.ts` (null for unknown focus, full transitive chain under the cap, one-hop fallback with `truncated: true` above it). `npm run test` — 57 suites / 434 tests, up from 55/400 before this session.

Verified against the real running app (local Postgres + `GITHUB_TOKEN`): re-analyzed `nielse63/repo-lore-ts-react-app-fixture` (0 edges — its `@/*`-aliased imports are still an unresolved gap from session 6, correctly not fabricated as calls) and `sindresorhus/globby` (212 named callables, 322 real resolved call edges, e.g. `getReadFileMethod -> bindFsMethod`, `processIgnoreFileCore -> createIgnoreFileReadError`) end-to-end via the real page: search, focus selection, the SVG diagram, and the right-rail "Called by"/"Calls" lists all rendered correct real data, including the honest zero-edge case for an isolated function. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all clean.

**Next smallest task:** Resume session 17 — deploy (product owner's account-creation steps, see previous entry). `search` remains the last fixture-scaffolded view needing its own scope review before real implementation. A Python call-graph follow-on ADR remains unscoped, deferred until there's a concrete reason to prioritize it over session 17 or `search`.

### 2026-08-26 — Data Flow's Chain tab reworked into an exports/entry-point-rooted tree (ADR-0012 amendment); Diagram view removed pending revisit

**Out-of-sequence refinement, at the product owner's direction**, of the Chain tab shipped the previous day. Validated end-to-end against a new fixture, `nielse63/repo-lore-ts-library-fixture` (a tiny plain-TS library: `math.ts` exports `add`/`multiply`/`divide`, `divide` calls `roundTo` from `internal/round.ts`, all re-exported from `index.ts`, which `package.json`'s `main` points at). Chain previously defaulted `focusId` to `callableSignatures[0]` and rendered `deriveCallGraphLayout`'s bounded BFS neighborhood as a flat, dagre-x/y-sorted list — a single function's local neighborhood, not a browsable overview, so only one arbitrary function ever showed up on load.

- `src/lore/call-tree.ts` (new) — `deriveCallTreeRootIds(signatures, edges, publicContracts, entryPoints)`: tree roots are the union of `PublicContract` exports (library-style repos, matched through re-export resolution via the contract's evidence location) and `EntryPoint`s with a resolvable `symbolName` (route/handler/server-action/CLI/bootstrap/UI-event — app-style repos), so the same view generalizes beyond plain libraries. Falls back to every fan-in-0 function when that union is empty, and further to every function when even that's empty (fully-cyclic graph), so the tree is never silently empty. `buildCalleesIndex(edges)` gives deduped caller → callee ids for on-demand expansion.
- `src/components/lore-shell/CallGraphContent.tsx` — Chain now renders a recursive `ChainTreeRow` per root, expandable in place (single click both focuses the row in the right rail and toggles its children, matching every other row on this page) rather than a single flat neighborhood list; a function already an ancestor in the same branch (recursion/mutual recursion) renders as a non-expandable "already shown above" leaf instead of looping. The old `deriveCallGraphLayout`-based flat chain and its "Step X of Y" right-rail header are gone with it. The Diagram view (`CallGraphDiagram.tsx`) is removed from `VIEWS` and no longer rendered — it added a second, differently-organized view of the same call data without yet proving useful against a real repo; revisit once the tree view has seen more real repos (see ADR-0012's amended Consequences). `CallGraphDiagram.tsx` and `call-graph-layout.ts` are left in place, unwired, not deleted. The List tab's "Sort By" pills now sit on the same row as "View Mode", right of it, instead of stacked below.
- `docs/architecture/decisions/0012-call-graph.md` — Consequences amended with both changes above.

New tests: `src/lore/__tests__/call-tree.spec.ts` (7 cases: export-only roots via re-export resolution, union with entry points, fan-in-0 fallback, fully-cyclic fallback-to-all, empty-callables, and `buildCalleesIndex` dedup/empty). `npm run test` — 60 suites / 453 tests. `npx tsc --noEmit` and `npm run lint` both clean; `npm run test:e2e` unaffected (12/12 passing — no e2e coverage of this page yet, per this doc's e2e scope note).

Verified against the real running app: `nielse63/repo-lore-ts-library-fixture`'s already-persisted analysis run rendered `add`/`multiply`/`divide` at the tree root, with `divide` expanding to reveal `roundTo`; View Mode showed only Chain/List with no Diagram option; List's Sort By pills sat beside View Mode.

**Next smallest task:** unchanged from the previous entry — resume session 17 (deploy), then `search`'s scope review, then the Python call-graph follow-on ADR.

### 2026-08-26 — UX/SEO/accessibility remediation from an external audit

**Out-of-sequence work, at the product owner's direction**, prompted by an external AI-generated audit of the live production deployment (`docs/architecture/ux-seo-a11y-remediation-plan.md` has the full accuracy review and design decisions). Verified every finding against source and the live/local DOM before fixing; one root cause was corrected (RL-001's real cause was `HistoryContent.tsx`'s client-side `new Date()`, not the audit's server-rendered-timestamp hypothesis).

Fixed: the History page's React hydration error (server-computed `nowIso` threaded through as a prop instead of computing `Date` client-side); Dependencies' tab keyboard focus (`Tabs` made controlled with explicit roving `tabIndex`) and row selection (a real `<button>` per row instead of an `onClick`-only `<tr>`); `generateMetadata` added to all five repository routes plus the root layout (`src/lib/route-metadata.ts`) — unique titles/descriptions, canonical, Open Graph/Twitter cards, `noindex, follow`; accessible names on the Dependencies/Data Flow search inputs; heading-hierarchy skips on Overview (`StepList` h4→h3) and Architecture (project-name heading no longer conditional on multi-project repos); a skip-to-content link in the lore shell; 12px→14px on table headers, source references, and evidence/limitation prose (left short badges and diagram node labels alone); named `aria-label`/`aria-labelledby` on both `<aside>` landmarks; dependency-count pluralization; and an "(opens in a new tab)" cue on external source links.

**Reverted during the session, at the product owner's explicit direction:** the audit's RL-010 (rename the Data Flow page to "Call Graph" per ADR-0012 item 6, which documented but never actually shipped that rename) was applied, then reverted back to "Data Flow" — the product owner wants "Data Flow" kept as the display name. `CallGraphContent.tsx`/`CallGraphDiagram.tsx` were renamed to `DataFlowContent.tsx`/`DataFlowDiagram.tsx` (and their exported component names) to match, since those two files are page-specific naming, not analyzer capability description; `src/analysis/js-ts/call-graph.ts`, `call-graph-layout.ts`, `CallableSignature`/`CallEdge`, and `MAX_CALL_GRAPH_NODES` are unchanged, since those describe what the feature technically computes (a call graph) rather than what the page is called. ADR-0012 got a dated addendum recording the reversal rather than a rewrite of its original decision/rationale text.

New/changed tests: two new `e2e/home.spec.ts` cases (onboarding copy, canonical/OG/Twitter metadata) — both DB-free, passing. No new Jest specs (the one pure-logic change, pluralization, is a one-line ternary matching the existing inline style already used elsewhere in the same files). **Disclosed gap, not closed in this session:** there is still no seeded-database Playwright fixture for `/lore/{owner}/{repo}/**`, so the DB-backed fixes above (tabs, row keyboard nav, search labels, heading order, landmarks) were verified by fetching real rendered HTML from a local dev server against two already-analyzed repositories (`nielse63/repo-lore-ts-library-fixture`, re-analyzed locally to match the audit's exact repo, and `stemmlerjs/simple-typescript-starter`) and checking the exact DOM properties the audit flagged, not by an automated regression test. `npm run test:unit` (453/453), `npx eslint src` (clean, one pre-existing unrelated warning), and `npx tsc --noEmit` all pass.

**Next smallest task:** unchanged from the previous entry — resume session 17 (deploy), then `search`'s scope review, then the Python call-graph follow-on ADR. Separately, and not blocking that sequence: build the seeded-DB Playwright fixture the disclosed gap above calls for.

### 2026-09-16 — Fixed a reported bug: re-analyzing a repo left the History page stale (GitHub issue #13)

**Root cause:** `reanalyzeRepository` (`src/app/actions.ts`) and `refreshHistory` were two entirely independent Server Actions. Re-analyzing persisted a new `analysis_runs` row and revalidated only `/lore/{owner}/{repo}`; `history_entries` (a single upserted cache row per repo, keyed on `repo_id` alone — see ADR-0010) was never recomputed or invalidated as a side effect. The History page kept rendering entries computed against whichever `Lore` was latest at the _previous_ "Refresh history" click, silently drifting from the repo's actual current analysis until a user separately visited History and clicked refresh.

**Fix:** when `reanalyzeRepository` produces an actual new run (`run.id !== previousRun?.id` — i.e. not ADR-0005's idempotent short-circuit on an unchanged commit), it now also calls `computeHistory` + `saveHistoryEntries` and revalidates `/lore/{owner}/{repo}/history`, inline in the same action. Two deliberate decisions, confirmed with the product owner: this recompute bypasses `claimHistoryRefresh`'s 60-second cooldown (that cooldown protects against rapid manual "Refresh history" clicks burning through the GitHub token; re-analysis is already separately rate-limited via `claimReanalysisAttempt`, and sharing the cooldown would just reintroduce the same staleness bug whenever a refresh had happened recently), and a history-recompute failure is logged but doesn't turn re-analysis's own success response into an error, since the structural re-analysis did succeed independently.

New/changed tests: `src/app/__tests__/actions.spec.ts`'s `reanalyzeRepository` suite now mocks `@/db/repos`, `@/db/history-entries`, and `@/history/compute-history`, and asserts history is recomputed/saved and `/history` revalidated only on an actual new run (not on the unchanged-commit short-circuit), plus a case confirming a history-recompute failure still reports `{ status: "done" }` while logging the error. `npm run test:unit` (475/475, up from 471), `npx eslint src` (clean, same one pre-existing unrelated warning as the prior entry), and `npx tsc --noEmit` all pass. `npm run test:e2e`'s one failure (`e2e/home.spec.ts`'s "explains scope" case, asserting copy — "Works with any public GitHub repository" — that no longer exists on the home page) pre-exists on this branch independent of this change and is unrelated to re-analysis/history; not fixed here.

**Next smallest task:** unchanged — resume session 17 (deploy), then `search`'s scope review, then the Python call-graph follow-on ADR.

### 2026-09-16 — Stale-analysis nudge on Overview (GitHub issue #12), scoped down from what the issue literally asked for

**Out-of-sequence work, at the product owner's direction.** Issue #12 asked to "force reanalyze" a repository automatically whenever its latest analysis is over a day old (guarded by a same-commit check). Ran `product-scope-guardian` against the issue before writing any code: it found the automatic-trigger shape — a page view silently kicking off a full re-analysis with no click — conflicts directly with `docs/product/mvp.md`'s and `docs/product/non-goals.md`'s explicit "no scheduled refresh" exclusion and this file's own restatement of it (session 11's log, and the README's Re-analyze section), regardless of whether the trigger is pull-based (page view) or push-based (cron/webhook) — from a visitor's perspective both are automatic, unattended re-analysis. It also flagged real operational risk: GitHub API calls and full tarball fetch/extraction cost tied to page-view traffic rather than to a per-repo cadence, with no worker/queue (ADR-0001/0004) to run it off the request path. Recommended not implementing the issue literally without resolving the conflict with the product owner first.

**Presented the conflict; product owner chose the scope-guardian's suggested alternative**: keep issue #12's staleness guard, but use it only to show a passive prompt pointing at the _existing_ manual "Re-analyze" action, rather than to auto-trigger anything. This stays inside the current MVP scope as written, so no ADR or non-goals.md amendment was needed (unlike ADR-0012's precedent, which did require amending non-goals.md, since that change deliberately expanded scope with the product owner's sign-off — this change deliberately does not).

- `src/helpers/is-analysis-stale.ts` (new) — `isAnalysisStale(analyzedAt, now?)`, pure, plus `STALE_ANALYSIS_THRESHOLD_SECONDS` (86400, a plain literal constant, matching `REANALYSIS_COOLDOWN_SECONDS`'s precedent rather than an env var). Exported from `src/helpers/index.ts`.
- `src/components/lore-shell/StaleAnalysisCard.tsx` (new) — dismissible card mirroring `PartialUnderstandingCard`'s existing shape (client component, local `useState` dismiss, no persistence across reloads — same as that precedent), styled with `PreviewBanner`'s token-based `tile-supporting` amber (informational, not `tile-alert` red, since an old analysis isn't a failure). Text points at "Re-analyze" rather than duplicating its server action. Issue #12's second guard (skip if the default branch's HEAD commit hasn't moved) isn't reimplemented here — it's already what `analyzeAndPersistRepository`'s ADR-0005 idempotency check does the moment "Re-analyze" is actually clicked, reporting "already up to date" per the existing `ReanalyzeButton` copy.
- `src/components/lore-shell/OverviewContent.tsx` — renders the card when `isAnalysisStale(snapshot.analyzedAt)`, in the same conditional slot as `PartialUnderstandingCard` right after the page header. Scoped to Overview only (the mission's "within 15 minutes of opening a Lore" framing centers on that landing page); Architecture/Dependencies/Data Flow/History each assemble their own top bar independently and don't get this nudge yet — a mechanical follow-up if wanted, not core to this issue.

New tests: `src/helpers/__tests__/is-analysis-stale.spec.ts` (5 cases: just under/at/past the threshold, a fresh analysis, and the `now` default). No Jest coverage for `StaleAnalysisCard`/`OverviewContent` themselves — this codebase has zero component-level Jest specs anywhere (UI is verified manually/via Playwright instead, and there's still no seeded-DB Playwright fixture for `/lore/{owner}/{repo}` — the same disclosed gap noted in the 2026-08-26 UX remediation entry). Verified manually instead: the project's `chrome-devtools` MCP browser couldn't reach `localhost` at all in this environment (connection refused on every host/port tried), so verification used an ad-hoc Playwright script (not a committed spec) driving the real local dev server against two already-persisted runs in the local Postgres — `nielse63/repo-lore-ts-library-fixture` (last analyzed 2026-08-26, 21 days stale): banner rendered with correct copy ("Analyzed 21 days ago."), dismiss button removed it; `nielse63/RepoLore` (analyzed today): no banner rendered. Screenshot confirmed placement/styling read correctly against the rest of the shell. `npm run test:unit` (487/487, up from 475), `npx eslint src` (clean, same one pre-existing unrelated warning as prior entries), `npx tsc --noEmit`, and `npm run test:e2e` (14/15 — the same pre-existing, unrelated `home.spec.ts` "explains scope" copy failure noted in the previous entry) all pass/unchanged.

**Next smallest task:** unchanged from before this session — resume session 17 (deploy), then `search`'s scope review, then the Python call-graph follow-on ADR.

### 2026-09-17 — Issue #12 revisited: passive banner promoted to a real (background, pull-triggered) re-analysis — ADR-0013

**Out-of-sequence work, at the product owner's direction.** The 2026-09-16 entry above shipped a deliberately passive version of issue #12: a banner, no automatic trigger. The product owner reconsidered: their actual intent for "no scheduled refresh" was narrower than the previous day's guardian-driven interpretation — it rules out _headless_ background jobs (cron, webhooks, polling with nobody visiting), not re-analysis that only ever runs because a real page view just happened. Asked two clarifying questions before touching anything (trigger shape; concurrency guard) rather than guessing, since this reopens real operational-risk tradeoffs the previous day's guardian review had specifically flagged. Answers: background/fire-and-forget (not synchronous — a stale visitor's page load must not pay for a possible tarball fetch/extraction), and reuse the existing per-repo claim as the concurrency guard rather than inventing a second one.

Wrote **ADR-0013** (`docs/architecture/decisions/0013-background-reanalysis-on-stale-revisit.md`) before changing code, since this narrows a documented non-goal and is exactly the class of decision `product-scope-guardian`/ADR review exists for (mirroring ADR-0012's precedent of amending `non-goals.md` with product-owner sign-off, rather than the 2026-09-16 entry's "stay inside current scope, no ADR needed" path). Amended `docs/product/non-goals.md`'s "scheduled refresh" bullet, `docs/product/mvp.md`'s "Explicit MVP Exclusions" list and Scope Tradeoff sentence, and `CLAUDE.md`'s own restatement, all to specifically name _headless_ refresh as the excluded thing and reference ADR-0013's bounded exception, rather than leaving language that reads as forbidding what this session now ships.

- `src/analysis/background-reanalysis.ts` (new) — `scheduleBackgroundReanalysisIfStale(owner, repo, analyzedAt)`: a no-op unless `isAnalysisStale`, otherwise schedules a re-analysis via `next/server`'s `after()` so it runs after the triggering response is already sent. Catches `RateLimitedError` silently (an expected outcome when a concurrent visitor's attempt — automatic or manual — already claimed the repo via `claimReanalysisAttempt`) and logs any other error rather than throwing, since nothing is waiting on this specific response.
- `src/app/lore/[owner]/[repo]/layout.tsx` — calls it for every non-`failed` run with a result, reusing the `run` this layout already fetches for the sidebar's visibility badge — covers every page under `/lore/{owner}/{repo}/**`, not just Overview.
- `src/components/lore-shell/StaleAnalysisCard.tsx` — copy changed from "Use Re-analyze above" to describe the automatic background check now actually happening ("Checking for a newer commit in the background — reload in a moment..."); the manual "Re-analyze" button is untouched and still works (and still honestly reports `RateLimitedError` if the background attempt already claimed the repo).
- `revalidatePath`/`revalidateTag` deliberately not called from the `after()` callback — `/lore/{owner}/{repo}/**` reads Postgres directly on every request with no Next.js fetch cache wrapping it, so there's no route cache entry to invalidate; a later visit just queries fresh.

**Caught during manual verification, not anticipated in the ADR draft:** the first working version of `background-reanalysis.ts` called `analyzeAndPersistRepository` directly, the same way `resolveRepository`'s initial-submission path does. That's wrong for this trigger specifically — it silently reopens GitHub issue #13 (History's cache going stale after a re-analysis) through a brand-new path, since only `reanalyzeRepository`'s Server Action had the History-recompute fix from the 2026-09-16 entry above, not `analyzeAndPersistRepository` itself. Fixed by extracting that fix into its own shared module, **`src/analysis/reanalyze-and-refresh-history.ts`** (`reanalyzeAndRefreshHistory(owner, repo) → { run, changed, historyRefreshed }`): re-analyzes, and only when `changed` (a genuinely new run, not ADR-0005's idempotent short-circuit) also recomputes and saves `history_entries`, reporting whether that recompute itself succeeded (`historyRefreshed`) separately from whether analysis produced a new run at all. `src/app/actions.ts`'s `reanalyzeRepository` and `src/analysis/background-reanalysis.ts` both now call this one function instead of duplicating (or, as briefly happened here, half-duplicating) the logic; `reanalyzeRepository` keeps the Server-Action-specific parts (`revalidatePath` for `/lore/{owner}/{repo}` always, `/history` only when `historyRefreshed`) that don't belong in a background context. Confirmed against real data the bug would have shipped: an earlier verification pass (before this fix existed) left `nielse63/repo-lore-ts-library-fixture`'s `history_entries.computed_through_sha` pointing at a superseded commit after a background-triggered re-analysis — exactly the issue #13 symptom, reproduced live by the pre-fix code and left as harmless, local-only stale test data rather than manually patched.

New/changed tests: `src/analysis/__tests__/background-reanalysis.spec.ts` (5 cases, updated to assert the callback invokes `reanalyzeAndRefreshHistory` rather than `analyzeAndPersistRepository`). `src/analysis/__tests__/reanalyze-and-refresh-history.spec.ts` (new, 6 cases: changed+refreshed, unchanged short-circuit skips refresh entirely, no-previous-run, a history-recompute failure reports `historyRefreshed: false` without throwing, an analysis failure propagates without attempting history at all). `src/app/__tests__/actions.spec.ts`'s `reanalyzeRepository` suite trimmed to mock `reanalyzeAndRefreshHistory` directly and assert only what this Server Action itself still owns (the `revalidatePath` calls and `ReanalyzeState` mapping) rather than re-testing history-refresh logic that now lives, and is tested, in the new module — avoids the same result being asserted at two layers. `npm run test:unit` (496/496, up from 487). No new Jest coverage for the `layout.tsx`/`StaleAnalysisCard.tsx` changes themselves, same disclosed gap as the previous entry (no component-level Jest anywhere in this codebase, no seeded-DB Playwright fixture yet) — verified manually instead, against the real local dev server + local Postgres + real `GITHUB_TOKEN`: requested `/lore/nielse63/repo-lore-ts-library-fixture` (stale since 2026-08-26) and confirmed the response returned immediately (not blocked on analysis) while a new `completed` analysis run appeared in Postgres seconds later with `repos.last_analysis_requested_at` updated (confirming the claim path was exercised), after which a reload showed "Analysis current · Analyzed just now" and no banner; repeated against `stemmlerjs/simple-typescript-starter` (stale since 2026-07-23, a real third-party public repo) to also capture the updated banner copy mid-flight via screenshot before its background attempt completed cleanly; re-verified the manual "Re-analyze" button still works correctly through the refactored `reanalyzeRepository` (reported "Already up to date" against the now-current commit). `npx eslint src` (clean, same one pre-existing unrelated warning), `npx tsc --noEmit`, and `npm run test:e2e` (14/15, same pre-existing unrelated `home.spec.ts` failure) all pass/unchanged.

**Next smallest task:** unchanged — resume session 17 (deploy), then `search`'s scope review, then the Python call-graph follow-on ADR.

### 2026-09-17 — Systems page scope review and design: ADR-0014, no code yet

**Out-of-sequence design work, at the product owner's direction.** The product owner asked to build the real Systems page (currently `notFound()` at `/systems`, with a fixture-bound reference UI parked at `_systems/`) and, initially, to model it on archify (github.com/tt-a1i/archify) for AI-driven codebase-to-diagram generation, extending afterward to Overview and Architecture.

Research before any design was drafted found archify isn't a code analyzer at all: its own documentation describes an AI coding agent authoring a typed JSON diagram spec interactively in chat, which archify then deterministically renders — there's no unattended backend to adopt, and nothing about it fits this project's synchronous, single-Next.js-deployable pipeline on Render. It was also confirmed that no LLM/AI SDK dependency exists anywhere in this codebase today, and that `docs/product/non-goals.md` currently excludes "AI-generated explanations or chat" outright, and that the mockups' Postgres/Redis "systems" require infrastructure detection ADR-0008 already rejected. Presented with these findings, the product owner chose: no AI anywhere in this feature; "System" = the existing Major Area (`StructuralArea`) concept, renamed and re-presented, no new analyzer capability; infra/data-store nodes dropped entirely; Systems ships first as its own scoped unit, with Overview/Architecture enhancements (in particular, ever surfacing the already-built-but-unexposed, no-ADR "Program Behavior Graph" — `src/analysis/shared/behavior-graph.ts` — as a "workflows" view) deferred to a future, separately-scoped proposal.

A `product-scope-guardian` review of the resulting concrete design (before any code was written) returned "ship, as scoped" with four required guardrails, all accepted: (1) it fact-checked that `AreaDependencyDiagram` (ADR-0009) — the component this design depends on reusing — is currently dead code (its only real call site, `ArchitectureContent.tsx`'s "Component Connections" section, has been commented out since commit `a9808aa`, 2026-08-25, with no recorded reason, and it was never wired into `OverviewContent.tsx` at all despite ADR-0009 stating it is) — the product owner chose to restore/wire it as part of this same change rather than build a second unverified consumer of it; (2) the classification labels must reuse `AreaDependencyDiagram.tsx`'s own existing "Implementation area" / "Tests / test support" / "Build/tooling configuration" vocabulary rather than a "Core"/"Primary" label, since that reads as an unevidenced criticality judgment `non-goals.md`'s "no generalized health findings" line already guards against; (3) point at `mission.md`/`non-goals.md` for any documentation amendment, since `docs/product/mvp.md` no longer exists (removed in commit `0691057`); (4) disclose that per-area subview slugs are stable only within one analysis run, since re-analysis can shift slug-to-area assignment.

Recorded as **ADR-0014** (`docs/architecture/decisions/0014-systems-page-real-data-scope.md`). No `non-goals.md` amendment is needed — nothing in this design extends interactivity, lowers diagram granularity below area-level, or adds a new inferred claim beyond what ADR-0008/0009 already permit.

**Next smallest task:** Implement ADR-0014: `src/lore/system-classification.ts` + slug helper (with specs) first (pure, easiest to verify in isolation), then restore `AreaDependencyDiagram` on Architecture and wire it into Overview (Decision item 6 — do this before the new pages, so Systems is built against a verified-live component), then `SystemsContent.tsx`/`SystemDetailContent.tsx` and the real `systems/[[...slug]]/page.tsx`, then delete `_systems/` and un-comment the Sidebar link. Session 17 (deploy) and `search`'s scope review remain unchanged, queued behind this.

### 2026-09-17 — ADR-0014 implemented: real Systems page, plus the diagram restoration it depended on

Built in the order the previous entry laid out.

- `src/lore/system-classification.ts` (new, + spec) — `classifySystem(area)`, reusing `AreaDependencyDiagram.tsx`'s existing "Implementation area" / "Tests / test support" / "Build/tooling configuration" wording verbatim rather than a criticality-sounding label, per the guardrail from the ADR's `product-scope-guardian` review.
- `src/lore/system-slug.ts` (new, + spec) — `assignSystemSlugs`/`findAreaBySlug`, kebab-case from `StructuralArea.name` with a numeric-suffix collision fallback in array order.
- `ArchitectureContent.tsx` — the "Component Connections" section (diagram + table) un-commented, restoring it to ADR-0009's originally-shipped state; it had been dead code since commit `a9808aa` (2026-08-25) with no recorded reason.
- `OverviewContent.tsx` — a new "How Areas Connect" section (the `AreaDependencyDiagram`, plus a "View full architecture" link) added after Major Areas, fulfilling ADR-0009's Decision item 6, which specified this but was never implemented.
- `src/components/lore-shell/SystemsContent.tsx` (new) — the list view: search-by-name, filter-by-classification (client-side, `"use client"`, mirroring `DependenciesContent.tsx`'s pattern), a right-rail `AreaDependencyDiagram` showing the full area graph, one row per Major Area (classification badge, owned path, relationship count, evidence count) linking to its subview.
- `src/components/lore-shell/SystemDetailContent.tsx` (new) — the subview: a single Overview section (Responsibilities, Owns, a `Relationships` `AreaDependencyDiagram` filtered to just this area's direct neighborhood, Key entry points), a right-rail "Evidence" citation list. No tabs (the mockup's Responsibilities/Relationships/Evidence tabs are collapsed into this one view, per the ADR); no "Appears in these flows" section and no Evidence-panel "Graph" sub-tab (dropped, not deferred — see the ADR's Decision item 4).
- `src/app/lore/[owner]/[repo]/systems/[[...slug]]/page.tsx` — rewritten from an unconditional `notFound()` stub to a real optional-catch-all: no slug segment renders `SystemsContent`, one segment renders `SystemDetailContent` for that slug, more than one segment is `notFound()`. Same fetch/`FailedRunPanel` pattern as every other real page.
- `src/app/lore/[owner]/[repo]/_systems/` (list, subview, `change-impact`) deleted — `change-impact` had no mockup-backed equivalent and wasn't migrated, per the ADR.
- `Sidebar.tsx` — Systems nav entry un-commented (`Package` icon, matching the ADR's routing decision); the adjacent comment about Repository Settings' still-stubbed `_repository-settings/` pattern updated so it no longer points at Systems as the "equivalent stub" example.

New tests: `src/lore/__tests__/system-classification.spec.ts` (5 cases) and `src/lore/__tests__/system-slug.spec.ts` (5 cases: kebab-casing, collision suffixing, the root-sentinel fallback, slug-to-area resolution, an unknown slug). `npm run test` (unit + e2e chained) — 70 Jest suites / 519 tests, all passing, and 16/16 Playwright e2e passing. `npx tsc --noEmit` and `npx eslint src` both clean.

Verified against the real running app (local Postgres + `GITHUB_TOKEN`, dev server): `nielse63/repo-lore-ts-library-fixture`'s already-persisted analysis run rendered two real systems (`/src`, `/src/internal`, both "Implementation area", 2 total supporting references) at `/systems`, each linking to a working subview (`/systems/src`, `/systems/src-internal`) showing real Responsibilities/Owns/Relationships/Key entry points and a populated Evidence rail; `/architecture` and the repo's Overview page (`/lore/nielse63/repo-lore-ts-library-fixture`) both now render the actual `AreaDependencyDiagram` SVG (confirmed via its `area-diagram-arrowhead` marker in the rendered HTML, not just the section heading), where before this session Architecture's diagram was commented out and Overview had none at all.

**Next smallest task:** resume session 17 (deploy), then `search`'s scope review, then the Python call-graph follow-on ADR — unchanged from before ADR-0014. Every repository analyzed before this change is unaffected (no `Lore`-shape or analyzer-version change was made — this is presentation-layer only, so no re-analysis or backward-compatibility default is needed, unlike ADR-0011/0012's model additions).

### 2026-09-17 — Test-coverage review for the Systems page; closed the e2e gap it had

**Gap found:** ADR-0014's own session entry above shows `system-classification.ts`/`system-slug.ts` got unit specs, but the two new page components (`SystemsContent.tsx`, `SystemDetailContent.tsx`) and the rewritten `systems/[[...slug]]/page.tsx` route had zero automated coverage of their own — consistent with this codebase's established "no component-level Jest, Playwright covers UI" split, but Playwright had nothing for Systems either. Two other things had gone stale in the same commit: `e2e/not-found.spec.ts`'s doc comment still described `/lore/{owner}/{repo}/systems` as unconditionally resolving through the old `notFound()` stub, and `README.md`'s Status paragraph and "Design system and UI routes" section still listed Systems (and Data Flow) as not-yet-real / deferred-and-unroutable, several sessions out of date. No relevant open GitHub issues (`gh issue list`) named Systems specifically.

**Fix:** added `e2e/systems.spec.ts` (9 cases), following `not-found.spec.ts`'s existing DB-backed pattern — reads the already-persisted `completed` run for the pinned private fixture `nielse63/repo-lore-ts-react-app-fixture` (`DATABASE_URL` only, no `GITHUB_TOKEN`), no new fixture/infra needed. Covers: the list renders real Major Area data (and that the retired `payments-service.ts` mockup text is gone), the Sidebar's now-restored Systems link navigates there, search and classification-filter both narrow the list with an honest empty state, a row click opens the correct subview (Responsibilities/Owns/Relationships/Evidence all present), the breadcrumb returns to the list, and both an unknown slug and a too-deep nested path 404. Verified each assertion against the fixture's real rendered HTML before asserting on it, rather than guessing at copy. Updated `not-found.spec.ts`'s stale comment and README's Status/"Design system and UI routes"/Testing sections to describe Systems (ADR-0014) and Data Flow (ADR-0012) as real, matching current routing, and to point at the new spec.

Verified: `npm run test:unit` (70 suites / 519 tests, unchanged), `npm run test:e2e` (25/25, up from 16), `npx tsc --noEmit`, `npx eslint e2e/systems.spec.ts e2e/not-found.spec.ts` all clean.

**Next smallest task:** unchanged — resume session 17 (deploy), then `search`'s scope review. The broader, long-disclosed gap (no Playwright coverage yet for Overview/Architecture/Dependencies/Data Flow/History's real-data rendering, only for 404 and Systems) is unchanged by this session and wasn't in scope — each would need its own pass to pick good real-data assertions the way this session did for Systems.

### 2026-09-21 — JS/TS path-alias resolution (GitHub issue #24, ADR-0015)

Resolved the long-disclosed gap ADR-0003 flagged since session 4/6: any alias-shaped import specifier (e.g. `@/components/Header`) was reported as an `unsupported` Gap rather than resolved, since `module-resolution.ts` only ever handled relative (`./`, `../`) specifiers.

New `src/analysis/js-ts/project-config.ts` reads and merges path-alias configuration from up to ten sources, per issue #24 and ADR-0015: `tsconfig.json`/`jsconfig.json` (via `ts-morph`'s re-exported `ts.readConfigFile`/`parseJsonConfigFileContent`, resolving `extends` chains — no new dependency), `package.json`'s `imports` field, and, parsed syntactically via ts-morph AST and never executed (per `docs/product/non-goals.md`'s "no executing analyzed-repository code" exclusion), `vite.config.*`, `webpack.config.*`, `rsbuild.config.*`, `rollup.config.js` (`@rollup/plugin-alias`'s `entries` shape), `nuxt.config.*`, `next.config.*` (only its `webpack(config) {...}` mutation pattern), and `.babelrc`/`babel.config.*` (`babel-plugin-module-resolver`'s `alias` option). A fixed precedence order resolves conflicts across sources; a computed/dynamic alias target (anything beyond a string literal or a `path.resolve`/`path.join` call with literal arguments) is left unresolved and reported as an honest Gap rather than guessed.

`module-resolution.ts`'s `buildModuleResolutionIndex` gained `rootDir`/`aliases` parameters and now resolves non-relative specifiers against the longest-matching configured alias through the same extension/index-file candidate logic already used for relative imports; `imports.ts` attempts this for every non-relative specifier before falling back to the existing alias-shaped-but-unresolved Gap, and cites the winning config file/key in its evidence. `extract-project.ts` wires `readJsTsProjectConfig` in once per run. `JS_TS_ANALYZER_VERSION` bumped `js-ts-v9` → `js-ts-v10` (ADR-0005) since previously-gapped aliased imports now resolve to real Relationships for many repositories.

New `project-config.spec.ts` (16 cases covering all ten sources, precedence, `extends`, malformed-file and computed-value Gaps); `module-resolution.spec.ts` and `imports.spec.ts` extended with alias-resolution cases. Four other call sites of `buildModuleResolutionIndex` (`call-graph.ts`, `react-effects.ts`, `react-events.ts`, `tests.ts`) updated for the new required `rootDir` parameter — they don't need alias awareness themselves, only relative resolution, so `aliases` is left at its default `[]`.

Manually verified against `fixtures/ts-react-app` (whose `tsconfig.json` declares `"@/*": ["src/*"]`): `App.tsx`'s `@/components/Header`/`@/components/Footer` imports, previously two `unsupported` Gaps, now resolve to real `depends-on` Relationships with evidence citing `tsconfig.json`.

Verified: `npm run test` (79 Jest suites / 619 tests, up from 70/519 — some of that growth predates this session; 43/43 Playwright e2e, unchanged), `npx tsc --noEmit`, `npm run lint`, all clean.

**Next smallest task:** unchanged from before this session — resume session 17 (deploy), then `search`'s scope review.
